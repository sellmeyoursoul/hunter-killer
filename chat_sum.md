# Handoff Context

**System State:** Working on branch `Goal_Movement_RefactorV3`, tracking `origin/Goal_Movement_RefactorV3`. Design/implementation work is tracked in `Project_Docs/Draft_Features/PHYSICS_SQUEEZE.md` (§9 has the staged implementation slice plan; §3 has numbered decisions; §11 is the changelog). Slices 1-7 of that plan are DONE (decisions 14-33). Full headless test suite (`"/c/Program Files (x86)/Steam/steamapps/common/Godot Engine/godot.exe" --path . --headless -s res://tests/run_all.gd`) is clean aside from one known pre-existing, unrelated baseline failure (`_test_creature_motor_stack_blocked_memory_writes`, a `live`-step-source heading bug) plus two known pre-existing SCRIPT ERRORs — none of this session's work caused or touched them. All working-tree changes are currently uncommitted (see `git status` — many `M`/`??` files across `creature/motor/`, `AI_int_lib/game_config_merge.gd`, `tests/run_all.gd`, `Project_Docs/`, plus new files `creature/motor/fall_physics.gd`, `creature/motor/ghost_obstacle_query.gd(.uid)`, `creature/motor/route_plausibility_scan.gd(.uid)`, `assets/creatures/wolf/wolf_3d.tscn`, and a renamed `fox_archetype.tres` → `wolf_archetype.tres`).

**Progress Made:**
- Fixed 5 live-playtest-discovered bugs in sequence, each diagnosed via logs/live headless probes before any code change, each with new regression tests, each validity-checked (temporarily disabled fix → confirmed new test fails → restored), each documented as a numbered §3 decision + §11 changelog row:
  - **Decision 28**: `MotorPathClear.has_clear_contact_path`'s C18 EAT-gate raycast went stale after shrubs migrated to the ghost/query-only layer (decision 25) — a wolf could "eat through" a shrub-refuge wall. Fixed by also raycasting the ghost layer.
  - **Decision 29**: `shelter_enclosure_blocker_mask` config default was stale (`8`, a retired real layer) — GK_SHELTER nomination/confirm for shrub refuges had been silent dead code since decision 25. Fixed default to `GhostObstacleQuery.GHOST_LAYER_MASK`.
  - **Decision 30**: C10 airborne-invariant crash (creature falls off a real ~13m cliff, exceeds flat 45-tick threshold) — per user's explicit spec, built a terrain-scaled invariant threshold: new `PlayfieldGroundSampler.max_elevation_range()`, new reusable `FallPhysics.ticks_to_fall()` module, `MotorPlane` computes `motor_invariant_max_airborne_ticks` from real terrain + gravity + a buffer at scale-time. Explicitly did NOT build cliff-avoidance/jump/fall-damage — left `FallPhysics` as a hook for later. Added 4 rows to `ENHANCEMENT_BACKLOG_PLAN.md` for cliff-avoidance/jump/fall-damage/push-attacks so the ideas aren't lost.
  - **Decision 31**: rabbit stuck oscillating against a boulder — `consult_precise_food`/`consult_coarse_bearing` had no way to exclude a food memory `apply_blocked_objective_resolution` had already given up on, so it immediately got re-picked every tick. Fixed by threading the existing `_food_pursuit_exclusions` helper (already used by live-food pursuit) into both memory-tier consult functions via `_sync_food_memory_objective`.
  - **Decision 32**: rabbit stuck spinning in place twice — `_try_maintain_pursuit_detour_latch` and its memory-pursuit sibling had no arrival check, so a frozen detour waypoint kept getting re-targeted even after the body arrived within centimeters of it. Fixed by adding an arrival-tolerance release to both latches.
- Investigated a 6th report (rabbit fled instead of sheltering when multiple wolves closed in) and determined it is **not a bug** — `MotorGoalHub.build_eligible_goals` never even adds `GOAL_SHELTER` to the candidate list while the acute Flight fast path is active, and `_flee_objective` has zero shelter/geometry awareness by design. This is exactly the not-yet-built §9 slice 9 ("flee-bias scoring integration," decision 20). Reported findings to the user, made no code changes, logged the investigation as a changelog row only.
- Landed **§9 slice 7** (no bug — a planned refactor-completion pass): **decision 33**.
  - Retired the carnivore's diet-role `+8` real `collision_mask` bit (9→1) — safe now that decision 28's ghost-layer raycast independently covers everything that bit used to gate, and nothing's been on real layer 8 since decision 25.
  - Converted `ShelterEnclosureProbe.enclosure_fraction` to decision 13's planned two-mode ring sweep: new optional `agent_radius`/`agent_height` trailing params (default off, fully backward-compatible) switch each ring sample from a zero-width raycast to a real capsule shape-cast sweep (`GhostObstacleQuery.sweep_capsule_along_segment`, generalized to take an explicit `mask` param instead of hardcoding the ghost layer).
  - Found and fixed a real edge case mid-implementation: a capsule already overlapping the blocker mask at the sweep's own origin can misreport as "fully clear" via `cast_motion`'s along-motion-only collision semantics — added an explicit `intersect_shape` overlap pre-check that short-circuits to fully-enclosed (`1.0`) instead.
  - Wired **Stage A only** (self-radius nomination, no threat) into all three real call sites: `_try_nominate_shelter_candidate`, `_begin_or_continue_shelter_eval` (`motor_planner.gd`), `_maybe_observe_shelter_opportunistically` (`creature_motor_stack.gd`). **Stage B (threat-radius hard-disqualify) deliberately NOT built** — deferred to slice 9, which needs a real threat context to test meaningfully.
  - 5 new/updated regression tests: `_test_shelter_enclosure_probe_shape_cast_catches_narrow_gap` (the money test — ray/rabbit clear a narrow doorway, wolf-scale capsule correctly blocked), `_test_creature_kinematic_body_real_mask_excludes_retired_diet_role_layer`, plus 3 existing test-fixture updates (C18's real-mask wall moved from retired layer 8 to real terrain layer 1; a stray shelter-eval test wall moved from layer 8 to the ghost layer; a single-blocker test's wall distance bumped 2.0→2.3 units to clear the new self-radius overlap pre-check). Both new pieces validity-checked independently (disabled → confirmed red → restored). Full suite run clean on 2 consecutive runs.
  - Updated `Project_Docs/Draft_Features/PHYSICS_SQUEEZE.md`: added decision 33 (full write-up), marked §9 slice 7 DONE, added 2 changelog rows (the flee-investigation non-bug finding, and slice 7 itself).

**Last Known Trajectory:** Slice 7 just landed and was reported to the user as complete, with the full suite clean. I then asked the user whether to proceed next with **§9 slice 8** (squeeze/choke-point belief type, decision 19 — smaller, self-contained, independent storage/decay logic) or **§9 slice 9** (flee-bias scoring integration, decision 20 — the larger mechanic that would actually make fleeing creatures consider nearby shelter, directly closing the gap the "fled instead of sheltering" investigation surfaced: race-margin scoring, widened candidate pool from 8-bearings-plus-nearest-shelter to every shelter/squeeze belief in radius, plus the fit-gate). The user has not yet answered which to start on — **that answer is the next thing needed before continuing implementation.** No uncommitted-work risk beyond the normal working tree; nothing is mid-edit or broken.
