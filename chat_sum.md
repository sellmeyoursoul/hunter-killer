# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Uncommitted diff, two files only: `creature/motor/motor_planner.gd`, `AI_int_lib/game_config_merge.gd`. Nothing committed this session. Headless suite (`"C:\Program Files (x86)\Steam\steamapps\common\Godot Engine\godot.exe" --path . --headless -s res://tests/run_all.gd`) verified byte-identical to unmodified `main` via `git stash` compare: **8 pre-existing assertion failures**, zero new regressions from this session's change.

**Progress Made:**
- Diagnosed a stuck-rabbit bug from a live runtime log at `%APPDATA%\Godot\app_userdata\Hunter Killer\logs\motor_explore_tick.log`: rabbit oscillated `TURN_L`/`TURN_R`/`MOVE_F` for 400+ ticks at `dist≈0.01` from food target `(26.0, 78.0)`, `food=0` throughout, calorie draining 82%→71% — consistent with user's description of a plant wedged between two rocks causing per-tick line-of-sight (LOS) flip-flop.
- Root cause: `_run_path_clearance_los_nav` in `motor_planner.gd` re-ran a raw single-tick LOS occlusion check every tick with **no debounce**. The check is a straight center-to-goal ray (`line_of_sight.gd::occlusion_fraction` — 10 sub-segments, `los_blocked_occlusion_fraction` default 0.80), not a facing-direction cast, confirmed by direct code read (not guessed). Grazing a rock edge flipped the verdict tick-to-tick, so `resolve_step_objective` alternated between the direct goal and a navmesh detour endlessly — matching the log's oscillation signature exactly.
- **Fix implemented** in `creature/motor/motor_planner.gd` — LOS-blocked hysteresis, mirroring the existing `consecutive_blocked`/`dead_end_record_min_blocked_ticks` debounce pattern already used elsewhere in the file:
  - New state fields `los_blocked_latched` (bool) / `los_verdict_streak` (int) added to `new_state()`.
  - New `_latch_los_blocked(state, motor_v3, raw_blocked)` helper — only flips the latched verdict after `los_hysteresis_ticks` (default 3) consecutive same-direction raw reads agree; otherwise holds the prior latched value.
  - `_run_path_clearance_los_nav` now computes `raw_blocked` and routes it through the latch instead of acting on it directly.
  - Reset the two new fields at every existing site that also resets `consecutive_blocked` alongside a fresh `step_goal` assignment (explore stuck replan, seek-fallback reseed, rim escape ×2 identical blocks, boundary scan start, pursuit detour mints ×2, flight-entry telemetry reset) — 8 reset sites total — so a stale latch from a previous objective never leaks into a new one.
  - New config default `"los_hysteresis_ticks": 3` added in `AI_int_lib/game_config_merge.gd`, placed next to `los_blocked_occlusion_fraction`.
- Verified no regressions: ran full headless suite before/after via `git stash`; identical 8 pre-existing failures both times (`_test_motor_planner_explore_move_not_falsely_blocked`, `_test_motor_planner_explore_post_scan_inward_align_no_flip_flop`, `_test_locomotion_executor_move_blocked`, `_test_memory_adapter_count_known_objectives_fractional`, `_test_motor_explore_seek_wall_bias_opens_away`, `_test_creature_motor_stack_food_map_confidence_inventory_ratio`, `_test_memory_adapter_ghost_danger_without_live_los`, `_test_creature_motor_stack_safety_blocked_by_ghost`) — pre-existing on `main`, unrelated to this change, explicitly out of scope for this task.
- Note: an earlier tool-result log line referenced a `motor_planner.gd:1934` comment dated "2026-07-16 duel review — rabbit stuck at playfield edge" — investigated and ruled out as a **different, already-fixed** issue (boundary/rim Flight deflection persistence bug, not this plant/rock LOS thrash).

**Last Known Trajectory:** Hysteresis fix is implemented, wired end-to-end, and verified clean against the full test suite. Was about to ask the user whether to also investigate the 8 pre-existing failing tests (explicitly out of scope for the original stuck-rabbit ask) or leave them alone — awaiting that decision. Nothing has been committed; both changed files are working-tree only.
