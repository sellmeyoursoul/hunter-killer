# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Working tree has uncommitted changes across 10 files + 1 new file (not yet committed — user has not asked for a commit). `"/c/Program Files (x86)/Steam/steamapps/common/Godot Engine/godot" --path . --headless -s res://tests/run_all.gd` is green (0 `ASSERT:` failures, exit 0), verified twice in a row after all changes below. Full implementation plan retained at `C:\Users\mikea\.claude\plans\synthetic-sleeping-horizon.md`.

**Progress Made:**
- **C18 fix (shipped):** `EAT` could complete through a solid the eater physically can't pass — `_can_eat_now`/`_is_within_eat_range` (`motor_planner.gd`) only checked straight-line distance + facing, no occlusion/reachability. Added `MotorPathClear.has_clear_contact_path(space_state, from, to, collision_mask, exclude_rids)` (`motor_path_clear.gd`, new, generic — reusable for future contact actions like combat), wired into `_can_eat_now` via a new `ctx`-threaded `_has_clear_contact_path_for_action` helper using the *eater's own* `collision_mask` (herbivore vs carnivore get different answers for the same shrub wall). New regression test in `tests/run_all.gd`; documented as **C18** in `CREATURE_MOVEMENT_V3_CLEANUP.md`. A test-harness lesson surfaced along the way: a dynamically-added `StaticBody3D` test fixture needs `await process_frame` **after** `queue_free()`, not just before assertions, or it lingers and corrupts a later unrelated test (tripped the C10 airborne invariant in `_test_motor_locale_approach_no_oscillation_smoke` until fixed).
- **RT4 documented:** added to `CREATURE_MOVEMENT_V3_RANDOMTESTS.md` — rabbit never paused to shelter in the debug shrub-refuge ring because `GK_SHELTER`'s feasibility was hardcoded `0.0` and its step-objective sync was a stub always returning `false` (fell through to undirected explore). Zero shelter-candidate-detection code existed anywhere pre-session.
- **Design correction mid-session:** initially recommended building shelter-candidate detection on `environment_cell_data.gd`'s `fit_size`/squeeze grid; deeper research proved that system is fully dormant at runtime (bake pipeline only called from tests, `art/env/` has no authored content, live grid is always synthetic `fit_size=-1.0`). Corrected course via `AskUserQuestion` — user chose the physics-layer ring-probe approach instead (same family as C18), staged as two slices (Slice 1 now, Slice 2 later).
- **Shelter Slice 1 (shipped)** — candidate detection → approach → STAY-evaluate → belief write → feasibility consult, per plan at `C:\Users\mikea\.claude\plans\synthetic-sleeping-horizon.md`:
  - New `creature/motor/shelter_enclosure_probe.gd` — `ShelterEnclosureProbe.enclosure_fraction`, an 8-ray full-circle fan probe (mirrors `LineOfSight3D.occlusion_fraction`'s idiom) measuring enclosure against a configurable blocker mask (default layer 8, the `MobBlocker` layer).
  - `motor_planner.gd`: split the old shared `GK_SHELTER, GOAL_REST` stub arm; `GK_SHELTER` now opportunistically ring-probes ahead of facing once per consideration cycle while unbound, binds a hit as a `"precise"` step objective (synthetic grid-cell instance id via `GoalSourceMemory.grid_indices_for_anchor`, deliberately reusing `"precise"`'s existing goal-kind-agnostic stuck/dead-end plumbing rather than inventing a new step-source tag), then STAY-evaluates for `shelter_eval_confirm_cycles` consecutive passing cycles once arrived (mirrors the `safety_met` consecutive-cycle idiom). `completed_step_objective` guarded so the multi-cycle STAY doesn't force re-consideration every intermediate tick.
  - `goal_belief_memory.gd`/`memory_adapter.gd`: dedicated shelter belief row (`upsert_shelter_row` — separate from generic `_upsert_row` since shelter's confirm/fail semantics must survive re-observation), `consult_shelter_beliefs`/`best_shelter_feasibility`/`count_confirmed_shelter_beliefs`/`shelter_candidate_recently_failed`/`record_shelter_evaluation`; `maintain()` gained shelter-specific TTL/radius keys and an LRU-cap eviction exemption for confirmed rows.
  - `creature_motor_stack.gd`: replaced the hardcoded `GK_SHELTER: return 0.0` feasibility stub with a real `best_shelter_feasibility` call; added `_try_complete_shelter_evaluation()` (EAT-outcome-hook analog) and `_shelter_map_confidence`/`_refresh_shelter_map_confidence()` (parallel to the existing food-inventory tracker).
  - `motor_goal_hub.gd`: fixed `_effective_base`'s `GOAL_SHELTER` arm, which was accidentally scaling by `food_map_confidence` (wrong signal) — now reads `shelter_map_confidence`, with a required `goal_shelter_explore_floor` bootstrap floor (without it, confidence starting at 0 and only ever becoming nonzero *after* shelter already won once would permanently lock it out of arbitration — traced and caught before shipping).
  - `game_config_merge.gd`: 13 new `motor_v3` tunables (probe radius/mask/thresholds, eval cycle counts, shelter-specific memory TTL/radius, bootstrap floor).
  - 8 new/updated tests in `tests/run_all.gd` (ring-probe unit test, nomination-binds-precise, eval-confirm progression, eval-fail-gives-up, feasibility-reflects-belief, effective-base bootstrap floor [replaces old food-confidence-coupled test], shelter-specific TTL, LRU-cap exemption). One test bug found+fixed during verification: initial blocker-ring test geometry (12 unrotated boxes, 0.6 size) left angular gaps that exactly missed half the probe's 8 sample rays — fixed by widening boxes to 1.2.
  - `REST`-the-action and Slice 2 (Flight consulting confirmed shelter beliefs to bias flee waypoints) explicitly deferred, not silently cut — `MotorAction.REST` is confirmed unreachable from `select_action` today, a separate pre-existing gap.
  - RT4 doc updated to `in_progress` with full fix write-up and what's still open.

**Last Known Trajectory:** User ran `/stop` right after Shelter Slice 1 shipped and both docs (`CREATURE_MOVEMENT_V3_CLEANUP.md` C18, `CREATURE_MOVEMENT_V3_RANDOMTESTS.md` RT4) were updated, then asked to update the `/stop` skill itself so it ends with a commit + push (`.cursor/rules/stop.mdc` step 3 added: stage all, commit with a summary message, push current branch, create upstream if missing, skip if nothing to commit, no force-push), then re-ran `/stop` under the new protocol — this session's changes (C18 fix + Shelter Slice 1 + doc updates + the stop-skill update itself) are being committed and pushed as part of this run. Next step on resume is starting Slice 2 (Flight/flee-waypoint shelter-bias) per the plan file's explicit "not yet started" scope note.
