# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`; uncommitted work spans **6e.1** (cone-only align, retire `turn_commit_sign`/`cmt`) and **6e.2** (per-objective consideration timer + clearance cadence). Motor pipeline: `sync_step_objective` → `resolve_path_to_step_goal` (cadence-gated) → `align_and_move`. Headless `tests/run_all.gd` green; tab check clean. Key files: `creature/motor/motor_planner.gd` (~1117 LOC), `creature/motor/creature_motor_stack.gd` (~634 LOC), `creature/motor/motor_planner_explore_log.gd`, `tests/run_all.gd`, `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3.md`.

**Progress Made:**
- **6e.1 closed:** Deleted `turn_commit_sign` / `_pick_turn_action` / commit seeding; `align_and_move` = pure §7.3.0 cone + fewest-turn + chaos at ±180°; `boundary_scan_sign` only for rim scan; retired HUD/log `cmt`; ~16 test asserts migrated to behavioral; doc §12.2 **6e.1** marked Done.
- **Duel manual smoke (2026-07-06):** Explore/rim movement much improved (minimal spin); one fox odd spin acceptable. Mutual acute Flight freeze (`ff=1`, both `gk=avoid_host`, `TURN_*` in place) diagnosed as predator prey-as-threat + per-tick flee retarget — **not** 6e.1 defect. Documented as **post-6d** slice in `CREATURE_MOVEMENT_V3.md` (§12.2 post-6d, §15 #17); explicitly out of 6e.1/6e.2/6d scope.
- **6e.2 closed:** `creature_motor_stack.gd` — `_ticks_since_consideration` replaces global `% interval`; `refresh_step_objective` / `run_path_clearance` on consideration ticks; immediate `_run_consideration` on EAT/arrival STAY; blocked `MOVE_F` → `apply_immediate_blocked_path_reevaluation`. `motor_planner.gd` — clearance gated by flag; 60° backtrack moved to §3.2 blocked path only; flee/live targets refresh on consideration only; `_maintain_explore_latch` between ticks. New tests: clearance gating, flee refresh cadence, blocked immediate reeval; updated consideration_advances. Doc §12.2 **6e.2** + §15 #16 marked Done.
- **6e.2 manual smoke:** User reports same flow as pre-6e.2 smoke — accepted as success given deferred Flight issue.

**Last Known Trajectory:** **6e complete** (6e.1 + 6e.2). User accepted duel smoke. **Next:** continue **6d** substeps (6d.1–6d.3 per build order) and/or implement **post-6d** (predator prey ≠ Flight threat, Flight fast-path locomotion). Optional: git commit branch work; add `ticks_since_consideration` to F10 HUD for easier cadence smoke. No commit requested this session.
