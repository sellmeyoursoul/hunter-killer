# Handoff Context

**System State:** `Goal_Movement_RefactorV3` branch. Hub doc: [CREATURE_MOVEMENT_V3_CLEANUP.md](Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md). Uncommitted diff across `AI_int_lib/game_config_merge.gd`, `CREATURE_MOVEMENT_V3_CLEANUP.md`, `chat_sum.md`, `creature/motor/creature_motor_stack.gd`, `creature/motor/locomotion_executor.gd`, `creature/motor/motor_planner.gd`, `creature/motor/motor_planner_explore_log.gd`, `tests/run_all.gd` — plus a new untracked `.claude/` dir (Skill files, see below). Nothing committed this session. Headless suite (`godot --path . --headless -s res://tests/run_all.gd`) is stable at **8 pre-existing assertion failures**, byte-identical across repeated runs (verify via `grep -c "ERROR: ASSERT"`, not the printed summary, which can undercount by one) — all 8 are documented, none are regressions from this session's work.

**Progress Made:**
- **R1 mitigation #1 (arrival damping)** shipped earlier and duel-verified: `LocomotionExecutor` tapers `MOVE_FORWARD` speed near the goal instead of post-hoc position-snapping; excluded `step_source == "live"` from damping after a same-day regression (damping only throttled the chasing creature, never the fleeing one).
- **R1 mitigation #2 (blend turn+move in one tick) shipped, architecture-wide:** new `move_blend_max_error_deg` (60°) config widens `align_and_move`'s `MOVE_FORWARD` gate (`_is_within_move_blend_arc`); `LocomotionExecutor._blend_turn_toward` rotates facing toward the target by up to one turn increment before moving (tank/car-style) instead of requiring full alignment first; `creature_motor_stack.gd` threads `step_goal` as the blend target on every `MOVE_FORWARD` tick, `live` step_source included. Fixed a real fox overshoot bug found via duel-log analysis (`dot` flipped 0.993→‑0.872 in one tick at contact, then `MOVE_FORWARD` kept firing facing away for ~10 ticks). 3 pre-existing tests hard-coded the old tight-cone threshold — updated to reference the new config key.
- **C8 — pre-existing test-failure audit (user-requested):** user asked whether a pre-existing, stable 13-assertion failure baseline (unrelated to R1, discovered while isolating mitigation #2's diff) was stale pre-V3 test debt. Audited all 13 individually — **none were testing removed functionality; all are live current systems.** Fixed 5 (test-fixture/threshold drift, not production bugs): explore_dir threshold impossible under the 8-wedge picker's geometry; single-tick cold-start couldn't clear the no-progress epsilon under acceleration ramping; two overshoot tests never set `state["goal_kind"]` so the goal-kind-change reset wiped their fixture; a flee-refresh test's fixture moved a threat along the same bearing, so an unchanged waypoint was actually correct. **8 left open**, documented in CLEANUP.md with root-cause hypotheses — most notable: `post-scan inward align` (boundary-scan re-arm, likely a real regression) and a shared LOS/wall-raycast non-detection issue affecting 3 tests.
- **C9 — flee-waypoint latch corruption, fixed:** user reported the rabbit got stuck at the playfield edge fleeing the fox, then both got stuck once the fox arrived. Traced via a live duel log (`motor_explore_tick.log`) to `apply_immediate_blocked_path_reevaluation` (`motor_planner.gd`) stamping its per-tick reactive backtrack/LOS deflection into the persistent 16-tick `flee_waypoint` latch — each blocked tick's deflection became the input to the next tick's own deflection, a self-referential drift that produced a stable ~34-tick, 4-point limit cycle at the boundary (net displacement ≈ 0), confirmed against the log (clean 16-tick latch drift far from walls vs. this cycling near them). **Fixed** by removing the stamp — the latch now stays pinned to its true mint; deflection is a this-tick-only movement correction. Added regression test `_test_motor_planner_blocked_move_reeval_preserves_flee_latch` (drives the deflection via the raycast-independent backtrack-memory branch to dodge the still-open C8 LOS-raycast flakiness). The fox's own related-but-distinct wall-oscillation (same `_run_path_clearance_los_nav` family, bounded not drifting since it has no persistent latch to corrupt) is **left open** as a C8 follow-up, not fixed here. Headless: stable 8/8 across 3 runs, zero new regressions. Full write-up in CLEANUP.md's new C9 section + changelog.
- **Claude Code Skills created** (`.claude/skills/go/SKILL.md`, `.claude/skills/stop/SKILL.md`), mirroring the existing Cursor `.mdc` protocol files (`.cursor/rules/go.mdc`, `.cursor/rules/stop.mdc`) so `/go` and `/stop` work natively in Claude Code. File structure/frontmatter confirmed correct (verified via research agent). **Not yet confirmed working**: skills are discovered at session start, not hot-reloaded, and these were created mid-session — this session never picked them up. `disable-model-invocation: true` only blocks *model*-initiated invocation, not user-typed `/stop`.

**Last Known Trajectory:** All motor-code work (R1 mitigation #2, C8 partial fix, C9 fix) is complete, headless-verified, and documented — nothing code-side is mid-flight. Two things outstanding for a fresh session: (1) **Verify the Skills actually registered** — this restart is the test; run `/skills` and confirm `go`/`stop` are listed, then try `/stop` directly (if `/skills` still doesn't list them, run `/doctor` — points to a workspace/trust issue, not the skill files). (2) **C9 duel re-verification** — no headless duel harness exists (§14.2.7 is manual-only), so confirm live that the rabbit no longer locks into a limit cycle at the playfield edge. Neither of these blocks starting new work; the 8 open C8 items (led by the likely-real `post-scan inward align` boundary-scan regression) are the next natural chunk of motor-code work if nothing else takes priority.
