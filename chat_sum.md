# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Uncommitted working tree, 8 files touched (nothing committed this session): `creature/motor/motor_planner.gd` (core changes), `creature/motor/creature_motor_stack.gd`, `creature/capabilities/creature_kinematic_body_3d.gd`, `main_3d.gd`, `AI_int_lib/game_config_merge.gd`, `creature/species/fox_archetype.tres`, `tests/run_all.gd`, `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md`. Headless suite (`"C:\Program Files (x86)\Steam\steamapps\common\Godot Engine\godot.exe" --headless -s res://tests/run_all.gd`) is at a **stable 6-assertion-failure baseline** (down from 8; two real bugs fixed and verified this session), re-confirmed clean after every fix iteration below.

**Important live-testing note:** headless test runs and live game sessions share the same `user://` log path (`%APPDATA%\Godot\app_userdata\Hunter Killer\logs\motor_explore_tick.log` and `godot.log`), and running headless tests overwrites/rotates that log — always run the live repro *last* before reading logs, not headless tests after a live session you still need to inspect.

**Active temporary instrumentation (still in the tree, intentionally not yet cleaned up):**
- `main_3d.gd`: `_DEBUG_FORCE_EDGE_CHASE_SPAWN := true` in `_spawn_duel_pair()` — pins the rabbit at the playfield edge (`herb_frac = (0.05, 0.5)`) and the fox on its interior side (`carn_frac = (0.20, 0.5)`) to force the fox-chases-rabbit-to-corner scenario on demand instead of waiting for chance overlap. **Must be reverted once C9/C10 investigation concludes.**
- `motor_planner.gd`: `ARR_DBG` print in `_maybe_locale_arrival_bind_or_clear` and `SYNCFOOD_DBG` print in `_sync_food_memory_objective`'s locale branch — both labeled with creature instance + physics tick, left in for the still-unresolved C2 locale-oscillation investigation (not touched this session, dormant).
- `NAV_DBG` print (was added to `_run_path_clearance_los_nav` to test a navmesh-instability hypothesis) has been **removed** — its question was answered (disproven) this session.

**Permanent changes shipped and verified this session (headless-confirmed, no regressions):**
1. **Real production bug fix** (`motor_planner.gd::note_outcome`): added `still_ramping` check (new `explore_last_move_disp_len` state field) so cold-start acceleration ramp no longer falsely escalates to a spurious explore-stuck replan. Fixed `_test_motor_planner_explore_move_not_falsely_blocked`.
2. **Test-fixture bug fix**: `_test_motor_planner_explore_post_scan_inward_align_no_flip_flop` — missing `state["goal_kind"]` seed + premature facing check before `apply_action`. Both fixed in `tests/run_all.gd`.
3. **Per-instance creature IDs**: `creature_kinematic_body_3d.gd` gained `creature_instance_id` (set from `get_instance_id()` in `_ready()`); `creature_motor_stack.gd::_creature_log_label()` now suffixes labels with it (e.g. `rabbit#6`, `fox#3033`) so logs stay unambiguous with multiple same-species creatures alive.
4. **C9 boundary-ping-pong fixes** (3 iterations, `motor_planner.gd`) — see below.

**Progress Made:**
- Root-caused and fixed the two real headless failures above (C8 tracker: 8→6 open).
- Investigated the "fox chases rabbit to map edge, both get stuck" live bug (new C9/C8 evidence). Built a forced on-demand repro (`_DEBUG_FORCE_EDGE_CHASE_SPAWN`) since it couldn't be triggered manually.
- Instrumented `_run_path_clearance_los_nav` with `NAV_DBG` to test the leading hypothesis (navmesh-first-waypoint instability) — **disproven**: 0 hits across a full repro session. Removed the print once its question was answered.
- Root-caused the real mechanism via tick-log tracing: (a) rabbit's `_maintain_flee_latch` unconditionally restored the blocked, wall-facing `flee_waypoint` every tick, discarding the one-tick escape deflection computed by `apply_immediate_blocked_path_reevaluation`; (b) fox's `_remint_alternate_pursuit_detour` alternates ±60° forever with no give-up condition when both sides are equally blocked at a corner (turned out not to be the active cause live — see below).
- **Fix iteration 1** (didn't work, live-tested): added `flee_backtrack_streak` counter; after 3 sustained backtrack deflections off the same latched waypoint, force an early `_mint_flee_waypoint()` re-derive. Live re-test showed no real change — `_flee_objective` is a pure bearing-away-from-threat calc with zero geometry awareness, so re-deriving from scratch reproduces almost the same wall-facing point when both creatures are stalemated.
- **Fix iteration 2** (partial improvement, live-tested): instead of re-deriving, *promote* the already-computed 60°-rotated deflection into the persistent `flee_waypoint` latch on escalation. Live re-test showed real short bursts of genuine forward progress (10-15 clean `MOVE_F` ticks, real `dist` reduction) between escalations — but tracing the wider log window showed it was still a closed loop, just bigger/slower (~30-40 tick period): the *natural* 16-tick latch expiry (independent of blocked state) re-derives via `_flee_objective` and happens to point almost exactly back the way it came (`err` flips ~180°), undoing the escalation's progress.
- **Fix iteration 3** (headless-verified, live result inconclusive due to new bug): extended the same backtrack-vs-recently-blocked-direction check into `_mint_flee_waypoint` itself, so *every* fresh mint (natural or escalated) gets checked, not just the reactive path. Live re-test: the rabbit's `dist` trended down cleanly and — for the first time this session — **the fox actually closed to `dist=0.00` contact with the rabbit**, meaning the pair converged instead of perpetually not-quite-meeting. Whether the flee-loop is now fully closed is unconfirmed, because a new bug interrupted the test at that exact moment (see below).
- Also added an escalating give-up fix to the fox's `_remint_alternate_pursuit_detour` (`pursuit_detour_escalation_count`, gives up and clears the detour latch after both ±60° sides fail once) — correct defensive fix, but turned out not to be the active cause in this scenario (`consecutive_blocked` never reached its 3-tick gate live, since the fox's oscillation was just downstream of the rabbit's own broken flee behavior).
- **New bug found (C10, not yet investigated):** right at the moment the fox reached `dist=0.00` contact with the rabbit near the forced-repro corner, both `motor_explore_tick.log` and `godot.log`'s `ARR_DBG` prints stop dead for the fox at the exact same tick (final logged state: `err=+180.0`, facing fully reversed). User reported the fox visually ended up under the playfield geometry, trapped, letting the rabbit escape. No script error printed — looks like a physics collision-resolution event on deep body overlap near a corner (two walls + the other creature's collider), not a crash. Possibly related to already-tracked C3 (EAT-contact range/facing gates) or C8 (physics/freefall-near-wall test failures), not confirmed. Full write-up with hypotheses and open questions is in the cleanup doc as the new **C10** section.
- All of the above (root-cause traces, all 3 fix iterations including the ones that didn't fully work, and the new C10 finding) are fully documented in `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md` — inventory table, C9 section (now `in_progress`, reopened), and new C10 section — per the user's standing preference that investigative work never be lost even when a fix attempt fails.

**Last Known Trajectory:** User explicitly chose to stop investigating C10 for now ("Document and pause here") rather than add Y-position instrumentation or keep pushing on C9 verification. Everything is written up and the todo list is clean/completed. Next session should either: (a) pick up C10 first since it currently blocks cleanly verifying whether C9's 3rd fix fully closes the boundary loop (add temporary Y-position/`is_on_floor` logging before the next repro attempt), or (b) if C10 turns out rare/unreproducible, do another live pass of the forced edge-chase spawn to directly answer C9's open "does the rabbit actually escape now, or just loop bigger" question. `_DEBUG_FORCE_EDGE_CHASE_SPAWN` should stay active until both are resolved, then get reverted from `main_3d.gd`.
