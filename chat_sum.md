# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Working through `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_DESIGNREVIEW.md` §1/§4/§5/§7 in agreed order §5 → §7 → §4 → §1. §5 (sentinel-bool convention) and §7 (threat-scan LoS/awareness hysteresis) are fully implemented and doc-updated. §4 (shared "stuck"/latch abstraction, scope narrowed to flee + pursuit-detour only — explore and boundary-scan explicitly excluded) is mid-implementation via a 5-substep validated plan; substeps 1-3 are done and headless-verified (0 assertion failures matching baseline each time). New shared primitive: `creature/motor/latch_hold.gd` (`LatchHold`, class scaffold: `is_active`/`start`/`decrement`/`escalate`/`clear`, keyed by `"<prefix>_ticks_remaining"` / `"<prefix>_escalation_tier"`). Both consumers (`_pursuit_detour_latch_valid`/`_clear_pursuit_detour_latch`/`_try_maintain_pursuit_detour_latch`/`_maybe_mint_pursuit_detour_latch`/`_remint_alternate_pursuit_detour` and `_maintain_flee_latch`/`_mint_flee_waypoint`/`clear_flee_waypoint_latch` in `creature/motor/motor_planner.gd`) now route their ticks-remaining/escalation bookkeeping through `LatchHold`; goal-specific payload (waypoints) and goal-specific policy (flee's scan-quality `flee_give_up_active` flag, pursuit-detour's `_remint_alternate_pursuit_detour` bearing math) deliberately stay outside the shared primitive. New `motor_v3` tunable `pursuit_detour_max_escalations` (default `2`, in `AI_int_lib/game_config_merge.gd`) replaces what was a hardcoded `> 2` literal — the cap is load-bearing (only safety valve against retrying forever on a visible-but-physically-unreachable live pursuit target; confirmed via tracing `_arm_prey_engagement_from_live_food`/`_tick_prey_engagement_latch`/`should_suppress_live_pursuit_blocked_resolution`). Field `pursuit_detour_escalation_count` was renamed to `pursuit_detour_escalation_tier` (pure rename, same semantics) to match the primitive's field convention. §4's discussion/decisions have NOT yet been written into the design review doc — exists only in this session's history so far.

**Progress Made:**
- §5 (sentinel-value/companion-bool convention): fully implemented across `motor_planner.gd`, `creature_motor_stack.gd`, `motor_explore_seek.gd`, and ~60 test call sites in `tests/run_all.gd`; found and fixed 2 latent bugs as a byproduct (unguarded `GK_AVOID_HOSTILES` `_flee_objective()` call; unguarded 22-way flee-scan `flee_waypoint` fallback read). Doc updated with gap enumeration + "Implemented" note.
- §7 (shared per-tick LoS/awareness hysteresis, narrowed to threat-scan only per user's explicit AskUserQuestion choice): added `AwarenessZone.latch_awareness_verdict` (generalizes `motor_planner._latch_los_blocked`'s single-target streak pattern to per-target dict keyed by `instance_id`), wired into `AwarenessZoneScan._scan_hostile_threats` via new `threat_los_cache` param (cache-pruned each scan for despawned/left-group ids), new tunable `threat_awareness_hysteresis_ticks` (default 3, separate from `los_hysteresis_ticks`), new `state["threat_los_hysteresis"]` field threaded through `creature_motor_stack.gd`'s `_run_live_scan`. Doc updated with classification table + 4 locked decisions + "Implemented" note.
- §4 substep 1 (contract + scaffolding, no wiring): created `creature/motor/latch_hold.gd`.
- §4 substep 2 (wire pursuit-detour, behavior-preserving): done and verified — see System State for details.
- §4 substep 3 (wire flee, behavior-preserving): done and verified — see System State for details.
- Resolved via discussion + code investigation (not yet written into the doc): explore and boundary-scan excluded from §4's scope (continuous progress-based re-eval and phase-machine shapes are poor fits); confirmed R1 (future continuous-controller slice, §1) is orthogonal to explore's arrival-distance re-eval; confirmed the shared latch primitive is goal-agnostic movement/pathing-layer infrastructure that doesn't need to change for future Combat/ranged-combat goals (those would use a different step-objective notion, goal-specific policy stays outside the shell); identified that dead-end recording is actively suppressed during live pursuit (`should_suppress_live_pursuit_blocked_resolution`) and no herding/dead-end-driven pursuit-targeting mechanism exists anywhere — marked as a **Slice 5 candidate topic** per explicit user instruction, not yet written into any doc.

**Last Known Trajectory:** Just finished and headless-verified §4 substep 3 (flee wired through `LatchHold`). Next up is substep 4 of the previously-agreed 5-step plan: a **test-validity pass** — confirm the specific flee-latch and pursuit-detour-latch named tests in `tests/run_all.gd` still assert meaningful behavior post-refactor (not just "still passing"), updating any that were asserting on stale/dead logic as part of this step (this was an explicit user requirement, not optional). After substep 4 is green, substep 5 is optional/deferred: revisit "always escalate on quiet pursuit-detour expiry" as a possible separate follow-up — only after 1-4 are independently done. Also still outstanding: writing §4's discussion outcomes into `CREATURE_MOVEMENT_V3_DESIGNREVIEW.md` (shape-comparison findings, explore/boundary-scan exclusion, escalation-bound parameterization decision, the 5 substeps, architectural-placement reasoning, Slice-5 candidate note) — deferred until 1-4 are fully resolved per user's own earlier instruction on the doc-location/Callable-vs-match/naming decision (§5-numbered sub-item, distinct from design-review §5). After §4 is fully implemented and doc-updated, sequencing calls for §1 (R1 executor-level continuous controller) next.
