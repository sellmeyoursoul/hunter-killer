# Handoff Context

**System State:** Godot 4.6 "Hunter Killer" project, branch `Goal_Movement_RefactorV3`. Two real motor-planner bugs were root-caused and fixed this session (C21, C22 — both documented with regression tests in `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md`). A large, separate design effort produced `Project_Docs/Draft_Features/PHYSICS_SQUEEZE.md` (new, untracked), which fully redesigns physics/size passability and shelter/squeeze belief mechanics — **every open question in that doc is now resolved or explicitly parked**, and its implementation-blocking status banner has been updated accordingly, though the user has not yet said "go implement." No PHYSICS_SQUEEZE.md code has been written yet — it is 100% design/doc work so far.

**Progress Made:**
- Fixed C21 (rabbit stuck between two boulders): added a bounded `live_food_awareness_grace_ticks_remaining` grace window in `creature/motor/motor_planner.gd`'s `_derive_find_food_step_objective`, config default in `AI_int_lib/game_config_merge.gd`, regression test in `tests/run_all.gd`.
- Fixed C22 (rabbit fled into the open instead of a known-safe shrub ring): added `_MotorAction.REST` to the action-check gate in `creature/motor/creature_motor_stack.gd` that triggers opportunistic shelter observation; regression test in `tests/run_all.gd`.
- Diagnosed and fixed the "bush turns white when eaten" issue live via Blender MCP — not a code bug, a Blender authoring/viewport issue (viewport shading mode + `scene.render.engine` was `BLENDER_WORKBENCH`, which never evaluates shader nodes; switched to `BLENDER_EEVEE`). User saved the `.blend` file themselves.
- Confirmed the first "stuck rabbit" report was NOT a bug (legitimate REST behavior).
- Created and fully developed `Project_Docs/Draft_Features/PHYSICS_SQUEEZE.md` (untracked, new file) — reconciled `OBJECT_AVOIDANCE_PLAN.md`'s stale "Completed" claims into a 3-bucket audit, then resolved all of §4's open questions in dependency order:
  - §4a: query-enforced passability (not physics-engine-enforced) via a shared movement-inert "ghost" query-only layer + live per-instance shape-casts for object-scale obstacles, existing `EnvironmentCellData` grid for field-scale effects (water/mud), `merge_greatest_impact` broadened for overlap composition.
  - §4b/§4c: moot (no size tiers needed once §4a is query-enforced).
  - §4d: automatic geometry-derived object sizing, no authored overrides for now.
  - §4e: `flight_just_entered`/`flight_just_exited` is the new `battle_tested` shelter-belief trigger (replaces old generic `safety_met` trigger), promotes directly from either `observed` or `confirmed`.
  - §4f: one shared dead-end-candidate check subroutine (not a mint-pipeline refactor — no common mint call exists today), called from each of 5 independent mint sites' tails; cheap early-out via existing `is_waypoint_dead_end`, then a belief-overlap override (any goal-kind belief at the same spot neutralizes the disqualification, no confidence threshold needed).
  - §4g: new choke-point/squeeze-gap belief type, two-tier only (`observed`/`confirmed`, no `battle_tested` — a gap's width is a physical fact, direct experience is already ground truth). `confirmed` stores the *passer's own known size*, not true geometric width (deliberate, enables a future pursuer-interception inference, not wired yet). Own storage bucket, not shelter's. Multi-mouth/tunnel topology explicitly out of scope.
  - §4h: `crush_weight` — confirmed by user as needing real design later, explicitly non-blocking, out of scope for this doc.
  - §4i: ground-truth-vs-noise (decision 8) reconfirmed; the noisy quantity at tactical decision time is a raw size/radius value, not a tier index or grid `fit_size` result.
  - §4j (the original motivating question): flee-bias is a bonus to bearing score, never an override; route-clearance uses a distance-only "who gets there first" race-margin (creature-to-point vs. worst-case threat-to-point), monotonic bonus/penalty, speed/cornering precision explicitly deferred; composure-driven noise scopes only to the binary fit judgment, not double-applied to the bonus; candidate pool widens from "8 open bearings + nearest shelter only" to "8 open bearings + every shelter/squeeze belief in that same radius," all scored through one shared function; same race-margin function reused for open-field bearings too.
  - Added §8, a code-grounded risk/trade-off review after all decisions landed. Two flagged as worth resolving *before* writing code: (8a) `NavigationServer3D`'s single shared navmesh bake (`main_3d.gd`'s `_bake_playfield_navmesh()`, no `geometry_collision_mask` set) was never reconciled with per-instance passability — could silently make every squeeze gap permanently unrouteable, or (if fixed) needs an explicit "size-mismatched creature paths into a gap and bounces off" story. (8c) `main_3d.gd`'s `_DEBUG_FORCE_OPEN_SHRUB_REFUGE_CLUSTER` (currently `true`) depends on the exact layer-8 trick being retired and isn't in the migration table — will silently go inert. Also flagged, lower-priority: (8b) LOS occlusion doesn't see the new query-only layer; (8d) continuous per-instance sizing + runtime-mutable size creates a flicker/hysteresis risk; (8e) no depenetration/escape-hatch story for query-enforced overlap edge cases; (8f) no policy for updating a `confirmed` squeeze belief when a second, differently-sized creature passes the same gap; (8g) no shared/herd belief propagation (a named trade-off, not an oversight); (8h) no staged-rollout/feature-flag story for a change this foundational to per-tick locomotion.

**Last Known Trajectory:** Design phase for PHYSICS_SQUEEZE.md is essentially complete — all of §4 is resolved, moot, or explicitly parked (§4h only), and a risk review (§8) has been added. The user had not yet said "start implementing" and had not yet responded to the two flagged pre-implementation questions (8a navmesh bake behavior, 8c debug-feature migration) before this `/stop` was invoked. Next session should either (a) get the user's call on 8a/8c and then begin implementation against the now-locked decisions in PHYSICS_SQUEEZE.md §3, or (b) continue design discussion if the user wants to revisit anything first. Do not start code changes against PHYSICS_SQUEEZE.md without an explicit go-ahead — the doc's own banner and prior user instruction ("make sure it is contained and documented before we start changing code") still apply until the user says otherwise.
