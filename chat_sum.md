# Handoff Context

**System State:** Godot 4.6 creature-sim, V3 motor stack (`creature/motor/*.gd`), driven by `Project_Docs/Draft_Features/PHYSICS_SQUEEZE.md` (size/passability/squeeze design + §9 slice plan) and `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md` (C-numbered bug/gap log). Headless tests: `"/c/Program Files (x86)/Steam/steamapps/common/Godot Engine/godot.exe" --path . --headless -s res://tests/run_all.gd`. Known baseline (pre-existing, unrelated, always present): `_test_creature_motor_stack_blocked_memory_writes` assertion + two SCRIPT ERRORs ("Nonexistent 'float' constructor", "Nonexistent function 'set_beliefs_for_test'"). Working tree is clean; branch `Goal_Movement_RefactorV3` is 13 commits ahead of `origin/Goal_Movement_RefactorV3` (not yet pushed this session).

**Progress Made:**
- PHYSICS_SQUEEZE.md §9 slices 1-10 all landed and tested; slice 11 (§8d held-belief hysteresis) deliberately parked — no live noised size estimate exists anywhere yet to apply hysteresis to.
- Fixed 3 distinct live "rabbit stuck" bugs found via smoke-testing, each a different mechanism, each the same shape (a safeguard exists but gets undone the same tick it fires): decision 40 (flee never consulted the shared dead-end check), decision 41 (locale food memory anchor had no exclusion mechanism at all), decision 42 (a live food's passibility-fail count got wiped by routine re-observation before the exclusion threshold could be reached).
- Implemented slice 10: Stage B shelter fit-gate (hard-disqualifies a shelter belief with any threat-sized breach) + decision 23's live detour-forcing bonus (route-plausibility scan now also shape-casts a threat's radius). New shared `_flee_relevant_threat_capsule` picks the *smallest* known in-awareness threat radius (caught an initial largest-radius bug via `choke_useful`'s precedent before shipping).
- Ran the user-requested §4k architecture review of the "stuck-target detection/exclusion" machinery (dead-end marks, passibility-fail counts, live-locale handoff exclusion, locale-arrival cooldown, pursuit-detour escalation): found 3 genuinely different concerns, not 5 duplicates; closed decision 21's mint-site wiring audit (dead-end check now wired into the pursuit-detour latch + its `memory_moving` sibling, `_mint_locale_search_waypoint`, and shelter's precise-candidate probe — the last 3 of 5 originally-named sites). Investigated merging the two bespoke cooldowns into the other mechanisms and found real correctness reasons not to (documented, not merged).
- Findings written up as `CREATURE_MOVEMENT_V3_CLEANUP.md` **C23** (this doc, not PHYSICS_SQUEEZE.md, is the home for this machinery per user direction — it's broader than size/passability). Fixed two other stale copies of decision 42's spec correction found during the doc-sync sweep (`CREATURE_MEMORY.md` §5.5, `CREATURE_MOVEMENT_V3.md`). Added PHYSICS_SQUEEZE.md's long-missing `PROJECT_DOC_INDEX.md` row.
- Discovered (via `git add -A` surfacing untracked files) a same-day Cursor→Claude-Code migration: root `CLAUDE.md` + nested `CLAUDE.md` files + `.claude/agents/*.md` (mirroring `.cursor/agents/*.md`, adapted with `tools:` frontmatter). User confirmed it's theirs; committed separately from the §4k work.
- Every fix/feature this session: regression test added, validity-checked (temporarily disable the fix, confirm the test goes red, restore, confirm green), full suite run clean apart from the known baseline, documented as a numbered decision + changelog row before committing.
- Commits this session, in order: `039c856` (flee oscillation, decision 40) → `e1da1de` (locale dead-end, decision 41) → `01ed2b6` (passibility-fail-count fix, decision 42) → `a4620b5` (status banner) → `0579e4c` (slice 10, decision 43) → `d8eeb65` (§4k review / C23) → `04cb07a` (Cursor→Claude-Code migration).

**Last Known Trajectory:** §4k review and slice 10 are both fully closed, tested, documented, and committed. User then ran `/stop`. No open task, no pending decision. Branch has NOT been pushed to `origin/Goal_Movement_RefactorV3` yet this session (13 commits ahead) — the `/stop` skill's own instructions call for pushing after committing `chat_sum.md`, so that push is the next mechanical step, not a design decision. Next natural work (if picked back up): whatever the user directs next — PHYSICS_SQUEEZE.md itself is now essentially feature-complete (slice 11 parked pending an unrelated noised-sizing feature), so a `/go` continuation should ask what's next rather than assume another PHYSICS_SQUEEZE slice.
