# Handoff Context

**System State:** `Goal_Movement_RefactorV3` branch. Hub: [CREATURE_MOVEMENT_V3_CLEANUP.md](Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md). **C6** (locale-consult precedence gap) — root-caused and fixed this session: `motor_planner.gd` `_sync_step_objective` never re-derived the food step objective when an incumbent `precise`/`coarse` belief was erased (stale `step_goal` latched forever, no fallthrough to the next memory tier). Added `_find_food_memory_tier_stale(...)` + wired elif branch to re-trigger `_derive_find_food_step_objective`. Status `done`; all three asserts in `_test_creature_motor_stack_memory_tier_precedence` green; headless full-suite diffed against pre-change baseline — zero regressions, failures dropped 15→13 (only C6's two asserts flipped). **C2 residual #2** (same-tick overshoot clamp) shipped: `LocomotionExecutor.apply_action` now takes optional `step_goal`; `_clamp_overshoot_to_goal` snaps the body back onto the goal (zeroes horizontal velocity) if a `MOVE_FORWARD` this tick carried it past the goal along the pre-move approach line — fixes the locale-anchor rear-hemisphere oscillation that the reactive Layer-1 remint (sub-meter close band) was too late to catch. `creature_motor_stack.tick` threads `_planner_state.step_goal` through on `MOVE_FORWARD`. Headless: `_test_motor_locale_approach_no_oscillation_smoke` green; `_test_motor_pursuit_pinch_detour_smoke` (C1) unaffected; full-suite diff shows zero new failures. CLEANUP.md updated in the same change set (doc-sync rule): C1/C2 inventory rows, C2 "residual #2" narrative, acceptance checkboxes (smoke green, no-regression), changelog entries. **C1 and C2 both still `in_progress`** — Pass 5 duel manual signoff is the one remaining gate for both.

**Progress Made:**
- **C6 root-caused + fixed:** `_find_food_memory_tier_stale` (precise/coarse/locale re-consult check) in `motor_planner.gd`; one root cause explained both previously-failing tier-precedence asserts, not two, confirming the doc's own speculation.
- **C2 residual #2 shipped:** same-tick position clamp in `locomotion_executor.gd` (`_clamp_overshoot_to_goal`) + plumbing in `creature_motor_stack.gd`. Verified via fresh `motor_explore_tick.log` capture that the reactive Layer-1 remint's close band (2 move-steps, sub-meter) was too small to catch the actual oscillation range.
- **Doc sync completed same change-set:** CREATURE_MOVEMENT_V3_CLEANUP.md — C6 section marked `done`, C1/C2 acceptance boxes updated, changelog entries added for both fixes.
- **New log review (2026-07-15 session):** latest duel ended `winner=herbivore cause=starvation_carn_herb_win herb_cal=35 carn_cal=0` — fox never landed an EAT. Reviewed the rolling `motor_explore_tick.log` tail (last ~800 lines / ~6.6s only): shows the fox perfectly aligned (`dot=1.000`) and moving forward every tick chasing a fleeing (`ff=1`) rabbit whose waypoint keeps advancing at roughly the same rate — a straight tail chase that never closes distance, not a fly-through/overlap-then-skip-EAT event. No `act=EAT` appears anywhere in the fox's log in this window.
- **User reported "fox moved past rabbit instead of EAT"** but log evidence in the visible window does not show that specific event (no close-approach moment captured; rolling buffer may have already discarded it). Pushed back per core.mdc "Observation vs code" — asked user to confirm whether they saw an actual collider pass-through, or whether it's this same non-closing tail chase, before opening a new cleanup item.

**Last Known Trajectory:** Awaiting user clarification on the fox/rabbit non-EAT report: (1) if it's a genuinely new pass-through/no-EAT bug distinct from C1–C6, it likely needs a new inventory item (candidate **C7** — possible pursuit-curve non-interception issue: live pursuit re-aims `step_goal` at the rabbit's *current* position every tick with no lead/intercept, so if rabbit flee speed ≥ fox chase speed the fox can align and move every tick and still never close distance); (2) if it's just this same never-closes-distance chase, the fix is likely a fox/rabbit relative-speed or lead-pursuit design change, not a repeat of C1/C2/C6. Next action: get a fresh log capture immediately after reproducing the reported behavior (current buffer is rolling/short), or get user's direct confirmation of what they visually observed, then decide whether to log a new C7 item in CREATURE_MOVEMENT_V3_CLEANUP.md before implementing anything.
