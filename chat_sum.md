# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. §4 (shared latch primitive) is fully complete and doc-updated (all 5 substeps + Slice candidates A/B written into `CREATURE_MOVEMENT_V3_DESIGNREVIEW.md`). Now deep into §1 (R1 executor-level continuous movement controller) per the doc's implementation sequencing (§5→§7→§4→§1). §1's design is fully pinned and documented; substeps 1-6 of its 7-substep implementation plan are **done and headless-verified**; substep 7 (acceptance: headless green ✅ done continuously, live historical-repro re-runs, open-ended smoke testing) is **in progress** — one live smoke-test bug already found and fixed this session.

Continuous controller model (all documented in `CREATURE_MOVEMENT_V3_DESIGNREVIEW.md` §1): turn is proportional-rate-capped (`move_turn_rate_deg_per_sec`, new tunable, default 1350°/s ≈ old `turn_increment_deg` cap at 60Hz) instead of a fixed per-tick step; forward speed is `max_speed * max(0, cos(heading_error)) * arrival_damping`, floored at zero (never goes negative — backward stays a strictly separate, discretely-triggered action, never derived from the continuous law). `align_and_move` (motor_planner.gd) now unconditionally returns `MOVE_FORWARD` once `step_goal_set`; the old `_is_within_move_blend_arc`/`move_blend_max_error_deg` TURN-vs-MOVE gate and `force_align_turn_before_move` flag are both deleted (confirmed via the "no staleness window" reasoning: `move_turn_target` is fed fresh from `step_goal` every tick already). `turn_increment_deg` survives unchanged for pure-orientation-only behaviors (boundary_scan, EAT-orbit) which stay out of §1's scope entirely.

Key files touched this session: `creature/motor/locomotion_executor.gd` (continuous turn+speed law), `creature/motor/motor_planner.gd` (align_and_move collapse, force_align_turn_before_move deletion + cascade cleanup of `_assign_resolved_step_goal`/`_apply_live_food_objective`/`_apply_locale_food_objective` signatures, explore-idle-stuck `still_aligning` exemption, coarse-tier `step_ultimate_pos` bugfix), `AI_int_lib/game_config_merge.gd` (new `move_turn_rate_deg_per_sec` tunable), `tests/run_all.gd` (~14 tests migrated/rewritten, 3 new coverage tests added). Full headless suite (`godot --headless -s res://tests/run_all.gd`) is green (0 assertion failures) after every substep; only 2 pre-existing baseline `SCRIPT ERROR`s remain, confirmed via `git stash` to predate this session entirely.

**Progress Made:**
- §1 substep 1: added `move_turn_rate_deg_per_sec` tunable (default 1350.0, derived to match old cap) + default-value test. Green.
- §1 substep 2: implemented the continuous law in `locomotion_executor.gd` (`_blend_turn_toward` now rate-capped via `delta` param, returns post-turn `max(0,cos(heading_error))`; `_displace_along_facing` gained `align_frac` param multiplying into `damp_frac`), still gated behind the old `align_and_move` branch (not yet wired). Green.
- §1 substep 3 (the real cutover): collapsed `align_and_move` to always return `MOVE_FORWARD`; deleted `_is_within_move_blend_arc`. 14 headless failures resulted, all classified and confirmed attributable to this cutover (11 direct discrete-mechanics assertions + 3 downstream carnivore-scan failures traced via bisection to a single culprit test, `_test_motor_planner_turn_alignment_no_flip_flop`, whose now-1-tick convergence shifted cross-test physics-frame timing — confirmed empirically, not assumed).
- §1 substep 4: deleted `force_align_turn_before_move` (field, 3 set-sites, dead read in `align_and_move`) plus its now-fully-dead support machinery (`_maybe_flag_material_step_goal_change`, `_is_continuous_objective_retarget`, and the `flag_material_change`/`body`/`motor_v3`/`delta` params threaded through `_assign_resolved_step_goal`/`_apply_live_food_objective`/`_apply_locale_food_objective` purely to support it) — confirmed via grep these were private with no external callers before simplifying all 8 call sites. 17 failures (14 carried + 3 new `force_align_turn_before_move` assertions), all expected/classified.
- §1 substep 5 (test migration): rewrote 12 tests across all 4 planned categories (discrete-mechanics assertions replaced with zero-floor/facing-alignment checks; dead-flag assertions deleted with fixtures adjusted so remint coverage isn't lost; ~6 simulation-loop-scaffolding tests rewritten to feed `move_turn_target` and assert monotonic alignment convergence instead of "spin via TURN ticks"; config tests confirmed unchanged) + added 2 new coverage tests (`_test_locomotion_executor_continuous_turn_rate_cap`, `_test_locomotion_executor_continuous_forward_speed_scales_with_alignment` — the latter needed a rewrite mid-implementation from raw-displacement to facing-alignment measurement after discovering `apply_horizontal_move_intent`'s per-axis `move_toward` acceleration ramp confounds raw displacement at 0 velocity). Suite fully green.
- §1 substep 6 (bucket-3 retuning): scoped the risk precisely (only explore's `explore_idle_stuck` check uses raw displacement; precise/locale/memory_moving's blocked-detection is purely collision-based, unaffected). Empirically confirmed a real near-miss (instrumented the rear-hemisphere 180°-misalignment test: `consecutive_blocked` climbed to 2/3 from legitimate slow-turning, not any real block) before fixing — added a `still_aligning` exemption (mirrors existing `still_ramping` idiom) that peeks (not writes) the already-existing `explore_last_facing_dot` field written by `_note_explore_align_progress` (an earlier draft double-wrote it and would have corrupted that function's own tracking — caught before landing). Post-fix, `consecutive_blocked` caps at 1. No config values needed changing.
- **Live smoke-test bug found + fixed (not a §1 regression):** a rabbit froze 321 ticks (~5.3s) in a corner, `act=EAT` looping without ever completing, calories draining. Root cause: `_sync_food_memory_objective`'s coarse-belief branch (motor_planner.gd) set `step_goal` but never cleared `step_ultimate_pos`/`step_ultimate_pos_set`, so a stale value from a prior objective let `_can_eat_now` (via `_resolve_eat_target_pos`, which prefers `step_ultimate_pos`) pass its facing/range gates against garbage — self-sustaining since EAT never moves the body. Confirmed unrelated to §1 (EAT bypasses `align_and_move` entirely; this gap predates the session). Fixed (coarse branch now clears both fields) + regression test added (`_test_motor_planner_coarse_clears_stale_ultimate_pos`) — verified the test genuinely fails without the fix and passes with it (an initial version was a false-tautology-pass because `state["goal_kind"]` mismatch triggered an unrelated goal-switch reset that masked the bug; fixed by seeding `goal_kind` to match). Suite green.
- All of the above is written into `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_DESIGNREVIEW.md`'s §1 section (implementation scoping, per-substep "Implemented" writeups, the live-bug finding) as the running design record — this doc is the source of truth for §1's status, not this file.

**Last Known Trajectory:** Just finished substep 6 and the live-bug fix/regression-test/doc-write cycle. §1 substep 7 (acceptance) is next: headless-green leg is continuously satisfied; still need (a) live re-runs of the specific historical repros named in the doc (C9's rabbit-stuck-at-playfield-boundary, the C1-C5 overshoot/orbit family) to confirm the continuous controller actually fixes them rather than just not-regressing, and (b) more open-ended live/duel smoke testing across goal kinds (flee, explore, shelter, rest, not just find_food) and playfield positions, per the three-legged acceptance bar already documented. User was about to choose between continuing live smoke testing vs. moving to the historical-repro leg when this session ended — ask them which they want to do first on resume, or suggest continuing live play since it's already producing high-value findings.
