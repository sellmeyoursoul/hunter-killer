# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Working tree is otherwise clean — this session made **no changes to tracked game/test code**; it was investigation-only. Untracked at session end: `.mcp.json` (new — Blender MCP server config, see below) and `environment/playfield_spawn_randomizer.gd.uid` (Godot-generated, leftover from a prior session). Both are about to be committed per explicit user instruction. Godot binary: `"/c/Program Files (x86)/Steam/steamapps/common/Godot Engine/godot" --path . --headless -s res://tests/run_all.gd`.

**Progress Made:**
- Investigated user report: "I've never observed the rabbit pausing to confirm the area is safe" in secure-circle test runs, even when fully fed. Traced the mechanic: `GOAL_REST` (`creature/motor/motor_goal_hub.gd:11,46-47`) requires `calorie_ratio >= 0.95` (hardcoded `_REST_CALORIE_FLOOR`, not a `motor_v3` key) **and** `safety_met` (5 consecutive threat-free consideration cycles, `creature_motor_stack.gd:700-707`). Confirmed via `CREATURE_MOVEMENT_V3.md` §1/§6.1 that the 0.90→0.95 "dead band" (eat urgency zeroes at 0.90 but REST doesn't gate in until 0.95) is **documented intentional design**, not a bug.
- Built and ran a throwaway headless probe (real rabbit body + real bush + real awareness scan, no threats, `body.current_calories` seeded to 90% of cap) to empirically test whether the mechanic actually fires. Result: **it does** — `calorie_ratio` climbed to 0.9817 via residual patrol/mapping urgency, `safety_met` went true, and `GOAL_REST` won at least one consideration cycle before the single test bush was exhausted and calories decayed back down. Mechanism confirmed sound; probe script was deleted after use (temp-copied into `tests/_scratch_rest_probe.gd`, removed post-run — not committed).
- Conclusion handed to user: REST's gating logic is not broken. Leading candidates for why it's not *observed* in real secure-circle runs: (1) `REST`'s action handler (`creature_motor_stack.gd:499`) is visually identical to `STAY` — no distinct animation, so even a winning REST cycle looks like ordinary idling without checking the debug HUD's goal-kind readout; (2) real playfield food density may not sustain `calorie_ratio` above 0.95 long enough, unlike the single-endless-bush probe; (3) `safety_met`'s 5-cycle window may not be clearing as often in a busier scene. Not yet root-caused to one of these — no live in-scene verification done yet (would need running the actual secure-circle scenario with the debug HUD, not just a synthetic probe).

**Last Known Trajectory:** User ran `/stop` to end the session (needs to sleep) and asked to commit (a) `chat_sum.md` and (b) the queued Blender MCP addition — `.mcp.json` (new MCP server entry for `blender-mcp` via `uvx`, no secrets) and the stray `.uid` file. No code fix was implemented for the REST-visibility question this session; next step on resume is likely running the debug HUD live in the actual secure-circle test scenario to pin down which of the three candidate explanations above is correct before touching any thresholds or REST's action feedback.
