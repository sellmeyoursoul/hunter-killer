# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. **Working tree is DIRTY — nothing from this session is committed yet** (9 files changed, 413 insertions / 20 deletions per `git diff --stat`). Files touched: `creature/motor/motor_planner.gd`, `creature/motor/memory_adapter.gd`, `creature/motor/occluded_in_zone_ghost.gd` (new function), `creature/motor/creature_motor_stack.gd`, `AI_int_lib/game_config_merge.gd`, `main_3d.gd`, `tests/run_all.gd`, plus `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md` and `.../CREATURE_MOVEMENT_V3_RANDOMTESTS.md`. Bug/cleanup tracking lives in those two docs (status vocab in `.cursor/rules/project-docs.mdc`). **Debug flags currently ON in `main_3d.gd` (verify before any real playtest/demo):** `_DEBUG_FORCE_EDGE_CHASE_SPAWN := true` (line 28, pins duel spawn near the west wall for C9/C16 repro) and `_DEBUG_FORCE_OPEN_SHRUB_REFUGE_CLUSTER := true` (line 44, force-spawns a refuge shrub ring). Both should probably be flipped back to `false` once repro work is done. Test workflow simplified this session: `_DEBUG_ASSERT_MOTOR_INVARIANTS` is no longer a global const requiring manual true/false flipping before `run_all.gd` — it's now a per-instance var (`_debug_assert_motor_invariants`, default `true`) with a `set_debug_assert_motor_invariants_enabled_for_test()` setter used only by the one test that needs it disabled. Godot binary used all session: `"/c/Program Files (x86)/Steam/steamapps/common/Godot Engine/godot.exe" --headless -s res://tests/run_all.gd` (full suite) and `.../tests/smoke_ai_player.gd` (3600-tick live-ish repro driver). Live playtest logs land in `C:\Users\mikea\AppData\Roaming\Godot\app_userdata\Hunter Killer\logs\hunter_killer.log` (huge, multi-session — grep `Main3D playfield:` boot lines to find session boundaries) and `motor_explore_tick.log` (rolling 800-line buffer, gated by `hunter_killer_debug/motor_explore_tick_log=true` in `project.godot`, already on).

**Progress Made (all uncommitted):**
- **C13/C14** (already done before this session's context window opened): rabbit-freeze-after-one-bite fix and a C10-invariant test-fixture fix — see `CLEANUP.md` for detail, not re-touched this session.
- **C15** (done): rabbit slow-starve eat↔phantom-locale-anchor loop. Fixed by extending `locale_revisit_cooldown_ticks` 90→300 and adding `MemoryAdapter.notify_locale_food_arrival_empty` (mirrors the existing success-write path with a `TIER_FAILURE` write) so a coarse locale cell's rank erodes from repeated empty visits, not just a cooldown timer.
- **Rabbit-refuge debug feature** (implemented, not yet live-verified): `main_3d.gd` `_spawn_open_shrub_refuge_cluster()` rings 10 `open_shrub_3d` instances around playfield center — open shrubs sit on a physics layer only the carnivore's mask includes, so the rabbit passes through freely while the fox is blocked. Crippled `growth_rate`/`max_calories` so it can't be a permanent camp.
- **RT1 follow-up flee-distance fix** (done): `_flee_objective`/`_mint_flee_waypoint`'s fallback both dropped a `× 0.5` — flee now targets the full edge of the (already playfield-scaled) `awareness_radius` disk instead of its midpoint, per explicit user direction ("should be at least to the edge of the zone").
- **RT3** (done): `main_3d.gd:_snap_playfield_props_to_ground()` now `queue_free()`s a prop instead of just warning when its ground raycast finds nothing beneath it — fixes perimeter boulders floating past the irregular terrain edge (confirmed via a user-spotted south-wall boulder, X≈25–66 at Z=-99.5).
- **C11 recurrence** (done): under randomized (non-forced) spawn, goal-hub incumbent flip-flopped `avoid_hostiles`↔`find_food` on an exact 16-tick metronome — root cause is a *different* mechanism than the original C11 fix: the always-aware sphere (`awareness_radius`, playfield-scaled to ≈15.9 units) is much smaller than the forward-cone reach (≈58.2 units), so a mid-range threat's visibility depends entirely on facing, and each goal's own turn sweeps the cone on/off the threat. Fixed by adding `OccludedInZoneGhost.project_facing_lost_threat_ghosts` — a sibling to the existing wall-occlusion ghost system, covering "outside the cone, LoS otherwise clear" (the existing one only covers "in zone, LoS blocked by geometry"). Keeps a recently-seen hostile registering as a danger sample for `goal_memory_ghost_horizon_sec` (0.4s, reused) after it drops out of the facing cone.
- **C16** (done, 3 iterations to get right — see CLEANUP.md for the full false-start): flee waypoint always projected the full `flee_dist` regardless of the chosen direction's actual navmesh-measured reach, so near a boundary it routinely targeted a point past the map edge (confirmed: -105.9 vs a -100 boundary). Fix clamps travel distance to `final_reach` — **but only when a real positive reach was measured**; clamping the "zero reach in all 22 tested directions" fallback too collapsed the waypoint to ~0 distance from the creature's own position, which made bearing math noise-dominated and tripped the C9 ping-pong invariant via a new mechanism (caught via smoke-test regression, fixed by leaving that one fallback unclamped/stable-heading instead). Also fixed a second overshoot source in the Flight-entry `HORIZONTAL_FORWARD` bootstrap fallback the same way. Verified: full suite clean 3×, 7 repeated `smoke_ai_player.gd` runs (forced edge-chase spawn) with 0 invariant trips. One accepted residual gap documented in CLEANUP.md: the fully-boxed-in fallback can still rarely overshoot (~4.7 units, 1-in-7 runs) but no longer causes thrashing.
- All fixes this session verified via full `tests/run_all.gd` (0 `ASSERT:` failures each time) and/or repeated `smoke_ai_player.gd` batches before being called done. Both `CREATURE_MOVEMENT_V3_CLEANUP.md` (C11 recurrence note, new C16 section) and `CREATURE_MOVEMENT_V3_RANDOMTESTS.md` (RT1 trace resolved, RT3 done) updated to match.

**Last Known Trajectory:** User ran `/stop` right after C16 was verified clean (7/7 smoke runs, full suite green) — no code work in flight. **Nothing this session has been committed** — first thing on resume should probably be reviewing the diff and committing (likely split by concern: RT3, C11 recurrence, C16, matching this session's own pattern of one commit per fix). Remaining known-open items per the docs: **RT3** just fixed but not yet re-confirmed live by the user; **C9** still `in_progress` (not formally `done`, now further exercised by C16's fix — worth more repro budget with `_DEBUG_FORCE_EDGE_CHASE_SPAWN` still on); **R1** architecture-risk `watch` item unchanged. Both debug flags (`_DEBUG_FORCE_EDGE_CHASE_SPAWN`, `_DEBUG_FORCE_OPEN_SHRUB_REFUGE_CLUSTER`) are live — confirm with the user whether to leave them on for more repro or revert before committing/shipping.
