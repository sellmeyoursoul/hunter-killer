# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`, pushed through commit `f481570` plus this session's uncommitted waypoint-chain work (staged for commit next). Movement V3 refactor continues; this session was diagnostic/bugfix-focused rather than a new §-substep.

**Progress Made:**
- **Diagnosed and fixed the "fox beelines across the whole map from rest" bug:** root cause was `effective_reach_toward()` in `creature/motor/awareness_zone.gd` — the `area_only` (rest-perception) branch returned the configured `awareness_radius` unconditionally, never checking it against actual target distance, so a resting creature had zero-gated omniscient detection of prey at any range. Fixed to gate on `dist <= area_r`. Verified via a locked opposite-corner headless repro (fox NE, rabbit SW) that showed continuous false "fresh sighting" before the fix and correct independent wandering after.
- **Tuned `assets/creatures/rabbit/pack_resources.json`:** `seek_priority_food_ceiling` 0.80 → 0.60 so GK_SHELTER can win over find_food/rest sooner (shared key also affects Eat urgency band — flagged to user, applied per their explicit instruction).
- **Committed and pushed** (`f481570`) the above plus pre-existing uncommitted session work: memory-pursuit detour for blocked moving-prey chases, `visited_path_memory.gd` (new) for explore-wedge "walked-but-empty" tracking, boulder 3x scale-up fix (capsule-edge-rounding climb exploit), shrub regrowth slowdown (5s→50s).
- **Diagnosed a second live report** (rabbit resting near shelter, clipped a boulder mid-flee-from-fox, drifted away from shelter instead of toward it): traced to flee's candidate-selection backtrack-avoidance (`blocked_approach_backtrack_dot`=0.55, `blocked_approach_memory_ticks`=45) vetoing the shelter-biased candidate for ~0.75s after any collision near that bearing. Rolling `motor_explore_tick.log` (800-line, reset every round) had already rolled off the actual moment — mechanism confirmed via code reading, not a captured trace.
- **Built `creature/motor/motor_waypoint_chain.gd` (new, uncommitted):** reusable primitive — `simplify()` reduces a raw `NavigationServer3D.map_get_path()` route to interior bends + endpoint; `advance()` steps a creature through it hop-by-hop with arrival-tolerance advancement. Root cause of the boulder-clip bug: flee's shelter-bias scoring already used real navmesh reach but then collapsed the winning path to `path[-1]` and drove straight at it, discarding the curve that made it reachable.
- **Wired the chain into flee** (`motor_planner.gd`, uncommitted): all flee candidate-scoring paths (6-way sweep, shelter-bias 7th candidate, give-up 16-way escalation scan) now carry the full path through to `state["flee_waypoint_chain"]`; `flee_waypoint` still drives latch timing/backtrack-history unchanged. New `_advance_flee_waypoint_chain()` helper resolves each tick's `step_goal` from the chain instead of jumping straight to the final point. Pursuit-detour (food/prey re-routing) has the identical single-hop shape and is a flagged, deliberately deferred follow-up using the same primitive (user chose "shelter-flee fix only" scope).
- **Tests added to `tests/run_all.gd`** (uncommitted): 4 pure-function unit tests for `MotorWaypointChain` (collinear collapse, real-bend preservation, hop advancement, empty-chain fallback) + 1 integration test reusing the existing "blocked" navmesh fixture (center-wall pinch) confirming a real detour produces a multi-point chain and the first hop is the interior corner, not the far endpoint.
- **Test suite status:** full `run_all.gd` run shows exactly 1 assertion failure (`_test_creature_motor_stack_blocked_memory_writes`), confirmed pre-existing (fails identically with/without both this session's fixes, isolated via `git stash`/patch toggling) — not caused by any change made this session, not yet root-caused.

**Last Known Trajectory:** User ran `/stop` right after the waypoint-chain work was verified test-clean. Nothing is broken; the new module + integration are uncommitted. Next session should: (1) commit the waypoint-chain work (motor_waypoint_chain.gd, motor_planner.gd changes, run_all.gd test additions) with a descriptive message, (2) decide whether to convert pursuit-detour to the same chain primitive, (3) optionally dig into the pre-existing `_test_creature_motor_stack_blocked_memory_writes` failure (unrelated to this session, still unexplained), (4) live-test the shelter-boulder-clip fix in an actual duel run since it was only headless-verified.
