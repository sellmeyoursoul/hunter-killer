# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`, pushed through commit `674b625` plus this session's work below (about to be committed/pushed). All changes are documentation + one implemented feature slice (dexterity-driven turn rate) + its supporting instrumentation. Full headless suite green (`godot --headless -s res://tests/run_all.gd`), verified across 4+ runs: exactly one pre-existing, non-deterministic `slot >= slot_max` ObjectDB flake (confirmed via `git stash` to predate this session), zero regressions.

**Progress Made:**
- **Stat-curve convention unified project-wide.** Investigated a live "rabbit spinning through an obstacle" smoke-test report → root-caused to `move_turn_rate_deg_per_sec` (1350°/sec flat for every creature) → scoped making turn rate dexterity-driven, pegged at stat 1/10/25 (183.7/691.6/1350.0 deg/sec). While designing that curve, found and reconciled a **second** existing stat curve (`CreatureStatCurve.saturating`, used by Composure's WAIT discount and Observation's prey-race giveaway) — determined the single-anchor `saturating` shape is a strictly weaker special case of a proper 3-peg curve, so **retired it entirely**: added `StatMath.peg_curve(stat_num, v1, v10, v25)` (reuses `stat_to_point`'s table ratios as a proportional remap) as the project's one stat-driven-scalar curve; deleted `creature/creature_stat_curve.gd`; migrated Composure and Observation to `peg_curve` (numerically preserved their live-tested behavior at stat 1/10/25 — same output, new mechanism); config keys renamed `_worst`/`_best`/`anchor_*` → `_at_stat_1`/`_at_stat_10`/`_at_stat_25` throughout. `SHARED_STATTOPOINT_PLAN.md` resolved and moved `Draft_Features/` → `Completed_Features/` (all incoming links fixed repo-wide); its one out-of-scope open item (UI stat-display cap) deferred to `ENHANCEMENT_BACKLOG_PLAN.md`.
- **Dexterity turn-rate unification implemented** (the actual §9 slice, not just documented): added `stat_dexterity` to `CreatureDefinition` (default 10, matching Composure/Observation's stub shape); new `creature_motor_stack.gd::_refresh_move_turn_rate` (per-tick, `peg_curve`-driven, explicitly leaves bodies with no `CreatureDefinition` untouched rather than degrading them); `locomotion_executor.gd::_rotate_facing` gained a `delta` param and now turns at `move_turn_rate_deg_per_sec * delta` instead of a fixed `turn_increment_deg` step — this is the actual mechanism unifying continuous movement with discrete boundary-scan/EAT-orbit turning onto one rate. `_boundary_scan_turn_budget` converts the per-creature rate to a tick budget via an assumed-60Hz constant (simpler than the originally-guessed real-time accumulator rewrite — numerically a no-op at defaults: `1350/60 = 22.5`, identical to the old fixed step). New `move_alignment_tolerance_deg` config key decouples path-clearance/LoS tolerance from turn mechanics. 5 new regression tests added. **Data change:** `rabbit_archetype.tres` gained `stat_dexterity = 25` (fox left at implicit default 10) — important asymmetry to remember: rabbit is *unchanged* from historical behavior (1350°/sec, same as always), **fox got slower** (691.6°/sec, down from the old universal 1350 flat default) — the visible maneuverability gap comes from the fox's reflexes dropping, not the rabbit's improving.
- **Flee gate/evaluation-window data-collection instrumentation added** (not the learned-bias mechanism itself, just raw logging to eventually tune it with real data instead of guessing): `creature_motor_stack.gd::_nearest_threat_debug_info()` mirrors `_flee_objective`'s exact nearest-threat selection; new `nearest_threat_dist`/`nearest_threat_id` fields in `get_debug_snapshot()`; `motor_planner_explore_log.gd` logs them as `tdist=`/`tid=` in the existing `motor_explore_tick.log` (gated behind `hunter_killer_debug/motor_explore_tick_log`, whenever any threat is in awareness, not just during active Flight).
- **Sketched, not implemented: learned evasive-turn bias for flee** (§9 of `CREATURE_MOVEMENT_V3_DESIGNREVIEW.md`) — reuses the `KindProfileMemory`/`stimulus_learn_registry` bandit substrate already proposed in §4 Candidate B. Reward signal converged (through discussion) on "distance-gained, gated on active pursuit" (mirrors the predator's own `_track_prey_race_and_maybe_give_up`) rather than binary escape/caught (too sparse) or raw distance-gain (would train straight-line flight, the opposite of intended). Three sub-questions answered: gate window = 16 ticks (matches `flee_waypoint_latch_ticks`); credit *every* currently-closing threat, not just nearest (cheap, piggybacks on existing per-tick threat iteration); evaluation window should be *shorter* than the gate window (~8 ticks guess) to avoid overlapping the next remint's own decision.
- **Multi-predator investigation spawned its own doc.** Answering "would multi-predator credit assignment even be testable" surfaced that `main_3d.gd` is **structurally 1v1** (`_carnivore_root`/`_carn_body` are singular typed vars, not arrays) — can't just spawn extra foxes as a data change. Also found the F10 debug HUD is hardcoded to `mobs[0]` (no multi-predator observability), while confirming `awareness_zone_scan.gd`'s threat scan and `motor_goal_hub.gd::urgency_flight` (max-over-samples) are *already* multi-predator-safe. Wrote up all of this as a new doc, `Project_Docs/Draft_Features/CM_V3_MULTI_MOBS.md` (registered in `PROJECT_DOC_INDEX.md`, which also picked up a missing registration for `CREATURE_MOVEMENT_V3_DESIGNREVIEW.md` itself along the way) — investigation only, no code changed for this part. Cross-linked from §9's learned-bias sketch as a dependency.

**Last Known Trajectory:** User called `/stop` right after the multi-predator doc was created and cross-linked — a clean pivot point, not mid-task. Nothing is broken; everything in this session (turn-rate implementation, stat-curve migration, instrumentation, both design docs) is uncommitted and about to be committed/pushed by this same `/stop` invocation. Next session should: (1) actually smoke-test the dexterity turn-rate change live (rabbit vs. fox maneuverability, watch for the fox-got-slower framing landing correctly, not misread as a new bug), (2) decide whether to add the `_flee_objective` nearest-only limitation as an explicit `ENHANCEMENT_BACKLOG_PLAN.md` row (asked, not yet answered), (3) if picking multi-predator work back up, start with `CM_V3_MULTI_MOBS.md`'s step 1 (scene/spawn structural change) since nothing else in that doc is testable without it, (4) once real gate/eval-window tick-log data accumulates from smoke sessions, revisit the 16-tick/8-tick guesses in `CREATURE_MOVEMENT_V3_DESIGNREVIEW.md` §9 with actual numbers.
