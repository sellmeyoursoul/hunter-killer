# Handoff Context

**System State:** Godot 4.6/4.7 game "Hunter Killer", branch `Goal_Movement_RefactorV3`. Multi-predator spawn (species-agnostic `playfield_spawn.creatures` config), generalized F10 debug HUD, and multi-threat flee blending from earlier this session are complete and live-verified (see `Project_Docs/Draft_Features/CM_V3_MULTI_MOBS.md`, all requirements marked done). Current focus: `creature/motor/motor_planner.gd`'s live-vs-locale food handoff (`_live_vs_locale_handoff_prefers_live`, C20 in `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md`) — defaults to live food, hands off to locale only for two survival exceptions (net loss; starvation risk via 2x round-trip cost vs. `food_handoff_starvation_margin_frac`), both now trait-modulated. All work this session is uncommitted — working tree is dirty, no commit made yet. Live user config (`user://game_config.json`, not in repo) is set to 1 rabbit + 3 foxes for playtesting.

**Progress Made:**
- Fixed the stuck-rabbit bug (C20): `_live_vs_locale_handoff_prefers_live` rewritten to default-to-live with two survival-only exceptions; new `_travel_calorie_cost` uses the real V3 flat-`move_calorie_per_sec` model (not dead `calorie_cost_per_unit_moved`). 3 regression tests + 2 pre-existing tests green.
- `change_stability` wrinkle closed: new `_food_handoff_starvation_margin(traits, caloric_needs, motor_v3)` scales Exception B's margin 0.5x-1.5x via `_StatMath.peg_curve`-style `t=(change_stability+100)/200` lerp (mirrors `_effective_prey_engagement_latch_ticks`/D10 convention). Exception A (net loss) deliberately left unscaled. New config: `food_handoff_starvation_margin_scale_min/_max`. Design locked via AskUserQuestion: "tune-only" (scale survival exception) chosen over "true preference override" to avoid reopening the same bug via a trait-gated path.
- `stat_observation` wrinkle closed: new `_food_yield_estimate_noise_frac(traits, motor_v3)` (uses `_StatMath.peg_curve(stat_observation, v1, v10, v25)`, defaults 0.4/0.15/0.03) and `_apply_food_yield_noise` jitter live and locale calorie estimates independently before exceptions are evaluated. Confirmed with user: stat 10 (both archetypes' default) carries moderate noise, not zero — real behavior change at default stats. Distance-estimate noise explicitly scoped OUT (every distance in this handoff is exact math off a known position, not a guess) and deferred as a separate future perception-noise system, reusable later for ranged combat.
- Caught and fixed a real plumbing gap: `creature_motor_stack.gd::_traits_from_body()` never included `stat_observation` in the traits dict fed to the planner — without the fix, the new noise code would silently use a hardcoded-neutral fallback for every real creature regardless of actual stat. Fixed.
- Caught and fixed a doc bug from the prior session: the C20 fix had been mislabeled "C11" in the changelog (C11 was already taken by the goal-hub flip-flop issue). Added a proper C20 table entry + full section in `CREATURE_MOVEMENT_V3_CLEANUP.md`, corrected the stale changelog line.
- Tests: added `_zero_food_yield_noise` test helper (C7 `goal_consideration_chaos` convention — noise must be zeroed for deterministic exact-outcome asserts); added `_test_motor_planner_live_locale_handoff_change_stability_scales_margin`, `_test_motor_planner_food_yield_estimate_noise_frac_pegs_stat_curve`, `_test_motor_planner_apply_food_yield_noise_zero_frac_is_noop`; zeroed noise in the 4 existing exact-outcome handoff tests. Full headless suite (`godot --headless -s res://tests/run_all.gd`) run 3x — only the known pre-existing non-deterministic ObjectDB flake (`slot >= slot_max`, floats between test locations each run), zero new regressions.

**Last Known Trajectory:** Both deferred wrinkles (`change_stability`, `stat_observation`) from the C20 stuck-rabbit fix are now fully implemented, tested, and documented — nothing left in-flight on the food-handoff feature. Distance-estimate/perception noise (flagged by user as possibly reusable for future ranged combat) is a real, tracked idea but deliberately NOT started — do not begin it without explicit user direction. No commit has been made this entire session; next step if resuming is either to commit this work or continue with whatever the user directs next in `CM_V3_MULTI_MOBS.md` or elsewhere.
