# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. §4 and §1 (substeps 1-6) of the Movement V3 refactor are complete and doc-verified. §1 substep 7 (acceptance) is in progress: headless-green leg is continuously satisfied; two live smoke-testing bugs have been found and fixed this session (both confirmed unrelated to §1's continuous-controller changes — pre-existing gaps surfaced by testing the new controller live). Historical-repro re-runs (C9, C1-C5) and broader live smoke testing across goal kinds are still outstanding.

**Progress Made:**
- §1 substeps 1-6 (continuous turn+move controller) — done, headless-verified, documented in `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_DESIGNREVIEW.md` §1 (see prior commit `8ae2fb7` for full detail).
- **Live bug #1 (fixed, prior commit):** coarse-belief-tier `step_ultimate_pos` staleness causing a 321-tick EAT freeze.
- **Live bug #2 (this session, fixed):** rabbit permanently stuck against a boulder pursuing an unreachable stationary bush. Root cause: `apply_blocked_objective_resolution`'s "seek" action (`blocked_objective_resolver.gd`) resets step state via `_seed_explore_after_seek` once a target's `passibility_fail_count` degrades its score, but the very next tick `best_ready_food_target` (awareness_zone_scan.gd) — which ranks purely by yield/distance — had no awareness of that failure history, so it immediately re-picked the identical unreachable instance and re-blocked, forever. Fixed by giving `best_ready_food_target` an optional `excluded_instance_ids` param and adding `motor_planner.gd`'s `_food_pursuit_exclusions` (reads `adapter.get_beliefs()`, excludes instances past `passibility_fail_switch_threshold` — reuses the existing tunable, no new config), wired into the two call sites that re-target stationary live food (`_sync_step_objective`, `_derive_find_food_step_objective`). Pursuit-detour (moving-prey) logic untouched — it already has its own give-up escalation. Two new deterministic unit tests added (`_test_awareness_scan_best_ready_food_target_excludes_ids`, `_test_motor_planner_blocked_food_excluded_from_reselection`) — a physics-driven repro was tried first but abandoned as flaky (small walls let the creature slip past; larger walls + 200+ ticks hit an unrelated pre-existing floor-fall instability in the test fixture), so the regression test seeds planner/memory state directly instead. Verified to genuinely fail without the fix and pass with it. Full headless suite green. Documented in the design review doc as a live smoke-testing finding.
- **Deferred, not yet investigated:** a second live finding — the 3D model and awareness-cone visuals rotate out of sync with the creature's real facing during movement (both read `last_move_direction` fresh with no obvious desync mechanism found on a static code read). Logged per user instruction, not yet dug into.

**Last Known Trajectory:** Both live bugs found this session are fixed, tested, and documented. Next: continue §1 substep 7's acceptance — live re-runs of the historical repros (C9 rabbit-stuck-at-boundary, C1-C5 overshoot/orbit family) to confirm the continuous controller actually fixes them, plus more open-ended live/duel smoke testing across goal kinds (flee, explore, shelter, rest) and playfield positions. User said "run stop now and then we can continue smoke tests" — resume directly into live smoke testing on the next session.
