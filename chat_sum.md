# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`, pushed through commit `f8c7c11` (prey-race giveaway + `.mcp.json` tweak) plus this session's uncommitted work below. This session was diagnostic/bugfix-focused (a live "rabbit stuck" report), not a new §-substep.

**Progress Made:**
- **Diagnosed and fixed the stuck-rabbit bug** via headless repro (`tests/smoke_ai_player.gd`) + new tick-log eat-gate instrumentation: a rabbit arriving at a `locale`-tier food memory anchor with no live-confirmed instance would clear the anchor, but a second call site in `_derive_find_food_step_objective`'s live-vs-locale handoff branch (`motor_planner.gd`) never checked the arrival cooldown the other locale-pick call site already respected — so it re-picked the *same* dead anchor every tick, forever (`STAY`, calories draining, read as "stuck" in ~19s of tick-log).
- **Fix has three parts (all in `motor_planner.gd` unless noted), per explicit user direction ("push explore/search around anchor; if nothing found, let belief go stale"):**
  1. Added the missing `_locale_anchor_on_arrival_cooldown` guard to the handoff branch (the actual bug).
  2. New bounded nearby-search: an empty locale arrival now arms a search window (`locale_search_ticks`=240 default / `locale_search_radius`=12m, in `game_config_merge.gd`) — mints random waypoints around the cell instead of idling or immediately wandering via generic `explore`. New state fields (`locale_search_anchor(_set)`, `locale_search_ticks_remaining`), new funcs `_mint_locale_search_waypoint`/`_maybe_search_arrival_remint`/`_clear_locale_search_state`, new `step_source = &"locale_search"`.
  3. Hard stale-out: `GoalSourceMemoryStore.invalidate_locale_belief_near` (`goal_source_memory.gd`) **erases** (not zeroes) the cell's belief row once the search window runs out empty — zeroing alone isn't enough since `consult_locale_seek`'s inactive-fallback matches on row *presence*, not `stored_strength`. Adapter passthrough in `memory_adapter.gd`.
- **Kept the eat-gate tick-log instrumentation** from the investigation (`ult=`/`uset=`/`wr=`/`fa=`/`cp=` columns in `motor_explore_tick.log`, gated behind existing `hunter_killer_debug/motor_explore_tick_log` project setting) — `debug_eat_gate_snapshot` in `motor_planner.gd`, wired via `creature_motor_stack.gd`'s `get_debug_snapshot`/`_last_planner_ctx`.
- **Added 3 regression tests** in `tests/run_all.gd`: search-window arming, give-up→invalidate, handoff-respects-cooldown. All passing; full suite at the same pre-existing baseline (1 known flaky failure, freed-ObjectDB-slot `"slot >= slot_max"` issue, non-deterministic call site each run — confirmed via `git stash` isolation to predate this session, untouched).
- **Separately investigated C10 (airborne/stuck-under-geometry)**, a pre-existing bug surfaced during repro (unrelated to the rabbit fix). Old capture (single-shot geometry probe only at the fatal 45-tick trip) was insufficient — probe reflected drift-landing geometry, not the actual liftoff point, and recoverable near-misses left zero trace. Added to `creature_motor_stack.gd`'s `_assert_motor_invariants`/`_airborne_geometry_probe`:
  - Liftoff snapshot (pos/velocity/floor-normal/geometry-probe) captured the instant `on_floor` first flips false, included in the fatal trip payload alongside the existing trip-time probe.
  - Velocity added to the existing 90-tick rolling trace.
  - `floor_below_slope_deg` added to the geometry probe (from raycast normal).
  - New non-fatal `MOTOR_INVARIANT_NEAR_MISS` `push_warning` log for airborne episodes that self-recover before the fatal 45-tick threshold (previously silent).
  - **Already caught one real near-miss in testing**: fox spawned, immediately fell 5.7m straight down (velocity purely vertical, action=REST, floor was flat 3.4° — not a slope-slide) through `AutoCollision_Grid`, recovered 27 ticks later. Points at an actual **collision-mesh gap near spawn**, not a movement/physics bug — likely correlates with the "no ground hit for prop — removing" warnings already seen in `godot.log` for boundary-decoration props at extreme playfield-edge coordinates. Not yet investigated further (would need `main_3d.gd`'s spawn/ground-sampling logic).
- **Full self-review pass done** (user asked "validate the code"): read every diff line-by-line, fixed one redundant `.duplicate()` in `invalidate_locale_belief_near`, confirmed no functional bugs. Re-ran full suite after cleanup — still exactly the pre-existing baseline.

**Last Known Trajectory:** User ran `/stop` right after the validation pass. Nothing is broken; all of this session's work (locale-search fix + tests, C10 instrumentation) is uncommitted. One open question posed to the user, not yet answered: if a live food target interrupts an active nearby-search via the *alternate* direct-apply branch in `_sync_step_objective` (rather than through `_derive_find_food_step_objective`), the search budget "pauses" (stops decrementing, doesn't reset) rather than clearing — minor timing imprecision, not a bug, but worth a decision. Next session should: (1) get the user's call on that pause-vs-reset question, (2) commit + push this session's work (locale-search fix, eat-gate tick-log columns, C10 liftoff/near-miss capture, all tests) — likely as two commits given the two distinct fixes, (3) if pursuing C10 further, look at `main_3d.gd`'s spawn/ground-sampling logic against the actual baked collision mesh coverage near playfield edges, informed by the captured near-miss data (and consider running more headless smoke repros first to build up a bigger sample of liftoff points before diving in).
