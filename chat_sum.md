# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Uncommitted working tree, 6 files modified (nothing committed this session): `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md` (all findings below written up in C9/C10 sections — read there for full detail/evidence), `creature/motor/motor_planner.gd` (C9 fix: geometry-aware flee-target scoring), `environment/playfield_bounds_3d.gd` (C10 fix: convex collision for obstacle props), `main_3d.gd` (navmesh-bake-ready signal wiring), `tests/run_all.gd` (updated stale boulder-collision test), `tests/smoke_ai_player.gd` (waits for navmesh bake before running ticks). `creature/motor/creature_motor_stack.gd` has **no net diff** — the live invariant-assertion harness (`_DEBUG_ASSERT_MOTOR_INVARIANTS := true`, added last session) is unchanged and still permanently on. `main_3d.gd`'s `_DEBUG_FORCE_EDGE_CHASE_SPAWN := true` still active (both still needed for repro). Headless regression baseline: 6 pre-existing `run_all.gd` assertion failures, byte-identical throughout this session, no regressions from any change made.

**Progress Made:**
- **C9 (flee-waypoint boundary ping-pong), 5 iterations this session:**
  1. Root-caused why the 3rd fix (from a prior session) still looped: `_mint_flee_waypoint`'s backtrack check used a **single-slot** `blocked_approach` memory + fixed +60° rotation, and the 0.55 backtrack-dot threshold sits just above `cos(60°)=0.5` — mathematically guarantees an exact 2-point resonance when cornered.
  2. **4th fix:** rolling multi-direction history (`state["flee_recent_dirs"]`, last 3 minted directions, 45-tick TTL) instead of a single slot; rotate past all of them (capped at 5 attempts). Broke the exact 2-cycle (confirmed: 17 distinct waypoints visited vs. 2) but the rabbit still made **zero net progress** near a corner — bearing rotation alone doesn't know if a direction is actually open.
  3. **5th fix (user-directed): geometry-aware candidate scoring.** New `_flee_candidate_reach` (`motor_planner.gd`) scores 6 candidate bearings by real navmesh-reachable distance (`NavigationServer3D.map_get_path`), reusing the same primitive C1's pursuit-detour already uses. Picks best-reaching candidate not in recent history.
  4. **Found and fixed a real headless-tooling bug along the way:** `smoke_ai_player.gd` was running the whole 3600-tick repro against a navmesh that hadn't finished its background-thread bake yet — every path query was silently empty. Added `Main3D.is_navigation_ready()` (`bake_finished` signal) + made the smoke driver wait for it (capped at 600 frames). This also means C1's own nav-substep pursuit-detour code was likely never genuinely exercised by headless testing before now.
  - **Result (verified, 4 repeated 3600-tick runs with navmesh actually engaged):** 2/4 completed with zero trips; 2/4 still tripped C9 but much later than any prior fix (tick 872, 2405 vs. ~500–1800 before). Real, substantial, honestly-reported improvement — **not fully closed**. No regressions.
- **C10 (fox falls through geography):**
  - User hit it live; disproved the original "contact-triggered" hypothesis from last session — falls happen early (~200–600 ticks), independent of prey proximity, always in the same small world-space neighborhood (X: −50 to −58, Z: −5 to −9).
  - Added temporary Y-position/velocity tracing, caught a partial fall in flight: smooth gravity-accelerated `vel.y` (gradual sink, not a teleport), self-recovered at a lower floor.
  - Wrote a throwaway headless raycast-grid diagnostic (deleted after use) directly against the physics collision surface (not inferred from telemetry). Found: **no hole** — terrain is a real continuous steep valley slope (Y drops from +1.3 to −6.7 over ~15m). But a **boulder's collision box sits embedded in that slope at the exact fall coordinates** — traced to scene node `Obstacles3D/@Node3D@595`, world pos `(-52.0, -1.55, -6.31)`.
  - Root cause: both the boulder (`AutoCollision_Cube`) and terrain (`AutoCollision_Grid`) used auto-baked **concave trimesh** collision (`PlayfieldBounds3D.supplement_trimesh_collision_from_meshes`, `mesh.create_trimesh_shape()`), and obstacle placement (`Main3D._snap_playfield_props_to_ground`) has no slope-awareness — un-tilted boulder trimesh on a steep slope leaves a seam a fast capsule can tunnel through.
  - **Fix shipped:** discovered food-plant shrubs already solve this correctly via `StaticObstacleCollision.sync_convex_blocker_from_visual` (real convex hulls, `mesh.create_convex_shape()`) — shrubs were never part of this bug class. Rerouted `PlayfieldBounds3D.ensure_obstacle_physics` (boulders only) through that same helper instead of the trimesh fallback. Terrain's own bake (`Main3D._mount_grasslands_floor`) deliberately left untouched — terrain must stay concave. Updated the stale `_test_boulder_obstacle_collision_bake` test to the new contract (`AutoConvexCollision` node name, asserts shape is genuinely `ConvexPolygonShape3D`) — this new assertion caught a real dev-time bug (Godot auto-names the shape child `@CollisionShape3D@2`, not `CollisionShape3D`; fixed by iterating children instead of fixed-name lookup).
  - **Verification (10 repeated 3600-tick headless runs, post-fix):** re-probed the boulder location directly — confirmed convex, smooth normals, spike gone. **9/10 runs had zero C10 trips** (real improvement over pre-fix rate). **1/10 still tripped** — `pos=(-57.56, -2.66, -6.09)`, tick 494, confirmed **~12 units from the nearest boulder** (no obstacle involved at all). This proves a **second, independent cause**: the terrain's own steep trimesh slope can apparently be tunneled through directly, with no obstacle seam needed. **C10 is not fully closed** — one real cause fixed and verified; a second remains, likely the same family as the already-tracked C8 "freefall after clipping through a thin wall" finding.

**Last Known Trajectory:** Just finished writing up the C10 fix + honest partial-verification result in `CREATURE_MOVEMENT_V3_CLEANUP.md` (status line, new "Fix shipped" subsection, acceptance checklist, inventory table) when the user ran `/stop`. No explicit next-step instruction was given. Two open threads, either could be picked up next: **(a)** C10's remaining terrain-only tunneling cause (no obstacle involved — likely needs CCD on creature `CharacterBody3D` bodies, or a deeper look at why a steep trimesh slope alone is tunnel-able; C8-family), or **(b)** C9's remaining ~50% intermittent-trip rate even with geometry-aware flee targeting (some corner configurations still leave every candidate bearing genuinely low-reach). Both `_DEBUG_FORCE_EDGE_CHASE_SPAWN` and the invariant harness should stay on until both close — they're the only reliable repro/detection tools for either bug. `tests/smoke_ai_player.gd` (now nav-bake-aware) is the correct way to headlessly re-verify either fix; remember to temporarily disable `_DEBUG_ASSERT_MOTOR_INVARIANTS` (creature_motor_stack.gd) only when running the full `tests/run_all.gd` suite (a pre-existing, still-unfixed incompatibility — synthetic fixtures with no floor false-trip the harness's C10 airborne check) — always revert that flag back to `true` afterward.
