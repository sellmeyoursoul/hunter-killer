# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`. Uncommitted working tree, 7 files modified (nothing committed this session — commits only happen when explicitly requested):
- `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md` — doc fully updated for all fixes below (summary table + individual C1/C2/C7 sections; eat-range playfield-scaling fix currently only documented via inline code comments + a new regression test, not yet given its own numbered entry like C10 got).
- `creature/motor/motor_plane.gd` — `eat_action_max_distance`/`arrival_tolerance` excluded from playfield distance scaling (new `_UNSCALED_MOTOR_DISTANCE_KEYS` const, checked before the existing scaled-key list in `_is_distance_motor_param_key`).
- `creature/motor/motor_planner.gd` — net diff is the C2 cleanup: removed temporary `ARR_DBG`/`SYNCFOOD_DBG` print instrumentation (`_maybe_locale_arrival_bind_or_clear`, `_sync_food_memory_objective`) and the now-dead `_dbg_label` helper. No other functional change (an EAT_DBG trace added mid-session for the eat-range investigation was added and fully removed again).
- `creature/templates/creature_carnivore_kinematic_3d.tscn`, `creature_herbivore_kinematic_3d.tscn` — `safe_margin = 0.06` on `Body` (C10 fix, prior session — untouched this session, listed for completeness).
- `tests/motor_replay_fixture.gd` — `MotorReplayFixture.drive_stack` is now a coroutine: new `_settle_on_floor` awaits 2 `physics_frame` cycles + runs a gravity-tick loop before driving the capture (C7 fix).
- `tests/run_all.gd` — two new regression asserts (eat-range/arrival-tolerance don't scale with playfield); both `drive_stack` call sites updated to `await` it.
- `creature/motor/creature_motor_stack.gd` — **no net diff.** `_DEBUG_ASSERT_MOTOR_INVARIANTS` was repeatedly flipped to `false` mid-session to run the full `tests/run_all.gd` suite (synthetic no-floor fixtures false-trip it) and reverted to `true` each time — confirmed back at `true` (correct) as of last check.
- Full-suite baseline reconfirmed multiple times this session, always identical: **6 pre-existing failures** (`MOVE_FORWARD against wall sets blocked`, `wall bias opens away`, 2× LOS-occlusion ghost-danger, `two known bushes` inventory_ratio, `belief north of origin` wedge scoring) — verified byte-identical across 5 separate `--headless` process invocations during the C7 fix verification specifically.
- Both `main_3d.gd`'s `_DEBUG_FORCE_EDGE_CHASE_SPAWN` and the invariant harness remain **on** — required until C9 closes (still open, untouched this session).

**Progress Made:**
- **Fox-can't-close-on-prey bug, root-caused + fixed:** `scale_creature_motor_v3_for_playfield` was scaling `eat_action_max_distance` (tuned 5m) down to ~0.53m on the actual duel arena (playfield min-edge ~200 units vs. the 1890-unit reference the value was tuned against) — made prey capture against live evasive rabbits nearly impossible. Confirmed via live `EAT_DBG` instrumentation showing `can_eat=false` at `eat_max=0.529` even at close range/good facing. Fixed by excluding fixed action/arrival-range keys from playfield scaling in `motor_plane.gd`. Verified: fox now lands `EAT` in the headless duel repro (`motor_explore_tick.log` shows `act=EAT dist=4.78`) — previously 0 EAT in 3600 ticks.
- **C1 (pursuit contact geometry stall, fox) marked `done`.** All headless acceptance items were already green; only duel-manual checkboxes were open. Closed on accumulated evidence — multiple live duel repros this session (C10 terrain work, the fox-can't-close investigation) exercised real pursuit-through-obstacle-geometry end to end with zero pinch-stall symptoms. No dedicated repro run; reopen if it resurfaces rather than filing new.
- **C2 (locale food approach oscillation, rabbit) marked `done`** on the same accumulated-evidence basis (user confirmed no rabbit food issues observed in recent play). Removed the temporary `ARR_DBG`/`SYNCFOOD_DBG` debug prints left in `motor_planner.gd` since 2026-08-05's residual #3 investigation (which never got root-caused, just stopped reproducing) — full-suite diff confirmed zero regression from the removal.
- **C7 (flaky headless assertions) — replay-fixture flake root-caused and fixed; tier-precedence flake still open.** `_test_motor_replay_fixture_drives_stack_from_capture` traced to a physics-frame/collider-registration race: this codebase only applies gravity / calls `move_and_slide()` inside a manually-driven `MOVE_FORWARD`/`MOVE_BACKWARD` action (never automatically per physics frame), so whether a freshly-spawned floor collider is broadphase-visible by tick 0 depends on real wall-clock scheduling during the test's single `await process_frame` — a genuine headless/uncapped-FPS race, not Jolt substep nondeterminism or the C4 ObjectDB noise (both original hypotheses tested and ruled out). Confirmed via a 12x in-process repeated-run harness (run 0 cold/thin-margin ~1.3, runs 1-11 warm/comfortable ~7.7). Two candidate fixes tried and rejected first (manual `move_and_slide()` loop with no engine yield; single `physics_frame` await) before landing on: two `physics_frame` awaits + gravity-tick settle loop in `MotorReplayFixture.drive_stack`. Verified deterministic across **5 separate `--headless` process invocations** (not just in-process loop iterations, which has its own confound from deferred `queue_free()` timing). `_test_creature_motor_stack_memory_tier_precedence`'s flakiness is a **different, untouched** cause (pure logic test, no physics bodies — C4 ObjectDB-noise hypothesis is the remaining untested lead).
- Doc fully updated for all of the above.

**Last Known Trajectory:** Just finished the C7 replay-fixture fix, updated the doc, and confirmed a clean `git status` (7 modified files, all intentional, no stray temp/debug artifacts) right before `/stop` was invoked. No commit made — user has not yet been asked whether to commit this batch. Natural next steps, none started: (1) decide whether the eat-range playfield-scaling fix deserves its own numbered CLEANUP entry (e.g. C11) the way C10 got one; (2) `_test_creature_motor_stack_memory_tier_precedence`'s flakiness (C7 remainder) via the C4 ObjectDB-noise lead; (3) C8's 6 still-open stable failures (LOS/wall raycast non-detection trio, ObjectDB-adjacent bush-inventory test, belief-wedge-scoring test) — untouched; (4) C9 (flee-waypoint latch, rabbit stuck at playfield edge) remains open/in_progress — do not remove either debug flag until it closes.
