# Handoff Context

**System State:** Branch `Goal_Movement_RefactorV3`, working tree clean, 3 new commits this session on top of prior C8/C7 work (`db62ff9` C10 recurrence fix, `7e1c288` C9 7th fix, `8f7b33b` C9 6th fix). Tracked bug/cleanup log is `Project_Docs/Draft_Features/CREATURE_MOVEMENT_V3_CLEANUP.md` (status vocab `open`→`design`→`ready`→`in_progress`→`done`/`wont_fix`/`watch`/`pending_recurrence`, also documented in `.cursor/rules/project-docs.mdc`). Current item statuses: **C7** `pending_recurrence` (no action needed unless it resurfaces), **C8** `done` (13/13), **C9** `in_progress` (flee-waypoint corner loop — see below, not formally closed), **C10** `fixed` (re-verified after a recurrence). Core files touched this session: `creature/motor/motor_planner.gd` (`_mint_flee_waypoint`, `_flee_has_visible_threat`, `_flee_candidate_reach`), `AI_int_lib/game_config_merge.gd` (new `flee_give_up_*` keys), `creature/templates/creature_{carnivore,herbivore}_kinematic_3d.tscn` (`safe_margin = 0.15`). Test workflow reminder: `tests/run_all.gd` requires temporarily flipping `creature_motor_stack.gd`'s `_DEBUG_ASSERT_MOTOR_INVARIANTS` `true→false` before running (known synthetic-fixture incompatibility, still unfixed), then reverting to `true` after — verify via `git diff` shows no change before any commit. `tests/smoke_ai_player.gd` (`godot --path . --headless -s res://tests/smoke_ai_player.gd`, 3600 ticks) is the forced-edge-chase repro driver, relies on `_DEBUG_FORCE_EDGE_CHASE_SPAWN := true` in `main_3d.gd` (still on, not yet removed). Latest verification: **10/10 fully clean headless repro runs** (zero C9 or C10 trips) — best result of the whole multi-session C9/C10 saga.

**Progress Made:**
- C9 6th fix (give-up escalation): `_mint_flee_waypoint` now escalates to a finer 16-direction full-circle scan (ignoring recent-backtrack history) when even the best of the existing 6 geometry-scored candidates reaches < `flee_give_up_reach_frac` (0.35) of the requested flee distance; latches shorter (`flee_give_up_latch_ticks`=5). Modeled on explore's `boundary_scan` but deliberately without its multi-tick turn-in-place pause — rejected during design discussion for failing the user's explicit "cornered animal sniff test." New config keys added to `game_config_merge.gd` + `CREATURE_MOVEMENT_V3.md` §7.3.2.
- C9 7th fix (flee-to-origin sentinel bug): root-caused and fixed a bug where `_flee_objective`'s `Vector3.ZERO` "no threat in awareness" sentinel was mistaken for a literal destination, sending the flee waypoint toward the world origin. Root cause: `awareness_zone.gd`'s threat-awareness LoS check has no hysteresis (unlike path-clearance LoS) and can flicker false near corner geometry, while `_flight_fast_path_active`'s latch keeps the Flight episode running regardless. Fixed via new `_flee_has_visible_threat(ctx)` helper — on a no-visible-threat tick, hold the existing latched waypoint instead of reminting from the sentinel.
- C10 recurrence dug into and re-fixed: a fresh headless trip (fox airborne near `(-60, -2.5, -7.3)`) was confirmed via a throwaway raycast-grid probe (written and deleted) to be the *same* cause-2 family as the original fix (terrain-vs-capsule tunneling on a ~30° slope, no boulder nearby, fox position ~0.5m below actual surface) — not a new mechanism. The original `safe_margin=0.06` fix was correct but insufficient at speed; bumped to `0.15` on both creature templates.
- Verified each fix independently (full `run_all.gd` suite clean each time, plus repeated `smoke_ai_player.gd` batches: 6/6 clean on the C9-specific check after the 7th fix, then 10/10 fully clean after the C10 margin bump). Doc sections/acceptance checklists/inventory table/changelog all updated to match. Commits split cleanly by concern (C9 fix vs. C10 fix, including doc hunks) per explicit user request to "commit both."

**Last Known Trajectory:** User ran `/stop` to end the session — no code work in flight. If resuming: **C9 is still `in_progress`**, not formally `done` (doc's own language: "not enough repro budget yet to call C9 formally done" — consider a larger repro batch, or accept current state and move to R1 architecture-risk work instead). **C10 is `fixed` again** but flagged with the caveat that margin tuning reduces-but-doesn't-guarantee-zero risk — worth a larger batch (20+ runs) before fully trusting it long-term. Other open items from C9's own doc checklist, not yet picked up: remove `_DEBUG_FORCE_EDGE_CHASE_SPAWN` from `main_3d.gd` once live-verified; remove `smoke_ai_player.gd`'s manual `_RUN_TICKS` tuning reliance; fix/scope the `run_all.gd`/live-invariant-harness incompatibility with synthetic no-floor fixtures (still worked around by hand every run, never actually fixed).
